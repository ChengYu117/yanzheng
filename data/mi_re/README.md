# MI RE Dataset

This directory stores the active dataset used by the Stage2/Stage3/Stage4 research pipeline.

- `re_dataset.jsonl`: RE samples
- `nonre_dataset.jsonl`: Non-RE samples

The old dataset copies under `NLP_data/` are legacy inputs and should be archived out of the main repository after migration is verified.
