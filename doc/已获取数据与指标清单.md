# 已获取数据与指标清单

本文档整理当前项目已经获取的数据、特征矩阵、实验指标和分析产物，并说明它们在研究中的作用。核心主线以 `outputs/misc_full_sae_eval` 为准；其他目录如 `outputs/gemma3_misc_layer_probe`、历史 `sae_eval_*` 和 `causal_validation_*` 作为辅助或旧阶段结果。

## 1. 当前正式主线

当前正式研究主线是：

> 使用 MISC 全量咨询行为单元作为样本，抽取 Llama-3.1-8B 中间层 hidden states，通过 OpenMOSS / LlamaScope SAE 得到 latent features，再分析 MISC 行为标签与 SAE latent 之间是否存在结构化多对多映射。

对应代码和输出：

| 类型 | 路径 | 作用 |
|---|---|---|
| 数据读取 | `src/nlp_re_base/data.py` | 读取 MISC jsonl，生成统一 records |
| SAE 主流程 | `run_sae_evaluation.py` | 抽取 hidden states、SAE features，计算结构和功能指标 |
| Mapping Structure | `run_misc_mapping_structure_analysis.py` | 分析 latent 与 MISC 标签之间的多对多结构 |
| 后续解释性 | `run_misc_interpretability_analysis.py` | 生成行为差异、质量差异、latent case cards |
| 候选因果 | `run_misc_causal_candidate_export.py` | 导出 G1/G5/G10/G20 latent 干预候选组 |
| 正式输出目录 | `outputs/misc_full_sae_eval` | 当前最重要的全量 MISC SAE 实验结果 |

## 2. 已获取的原始数据

### 2.1 MISC 全量标注数据

| 数据 | 路径 | 当前规模 | 作用 |
|---|---|---:|---|
| MISC 行为单元标注 | `data/mi_quality_counseling_misc/misc_annotations/high/*.jsonl` | 153 个 high 文件 | 主实验输入，高质量会话中的行为单元 |
| MISC 行为单元标注 | `data/mi_quality_counseling_misc/misc_annotations/low/*.jsonl` | 99 个 low 文件 | 主实验输入，低质量会话中的行为单元 |
| 会话质量标签 | `data/mi_quality_counseling_misc/metadata/labels.csv` | high / low | 用于质量差异分析 |
| 数据来源说明 | `data/mi_quality_counseling_misc/metadata/SOURCE_README.txt` | 1 个说明文件 | 记录数据来源与处理背景 |
| URL 元数据 | `data/mi_quality_counseling_misc/metadata/urls.csv` | 1 个 CSV | 保留原始会话来源信息 |

每条 MISC 输入样例来自 `misc_annotations` 中的 `unit_text`：

```json
{
  "file_id": "high_001",
  "unit_text": "hey Monica how are you doing today",
  "predicted_code": "QU",
  "predicted_subcode": "QUO",
  "rationale": "...",
  "confidence": 0.86
}
```

代码中使用 `src/nlp_re_base/data.py::load_misc_full_records()` 将其标准化为模型输入样本。

### 2.2 已标准化的实验样本

| 文件 | 路径 | 数量/形状 | 作用 |
|---|---|---:|---|
| 数据摘要 | `outputs/misc_full_sae_eval/dataset_summary.json` | 6194 条样本 | 全量样本数、RE/NonRE、标签分布、质量标签分布 |
| 标准 records | `outputs/misc_full_sae_eval/records.jsonl` | 6194 行 | 每条样本的统一记录，用于复现实验和追踪案例 |
| 标签矩阵 | `outputs/misc_full_sae_eval/label_matrix.csv` | 6194 行 | 每条样本对应哪些 MISC 标签 |

当前样本总览：

| 指标 | 数值 | 作用 |
|---|---:|---|
| 总样本数 | 6194 | 当前正式实验规模 |
| RE 样本 | 1358 | 二分类 RE vs NonRE 的正类 |
| NonRE 样本 | 4836 | 二分类 RE vs NonRE 的负类 |
| high quality | 4169 | 会话质量差异分析 |
| low quality | 2025 | 会话质量差异分析 |

标签分布来自 `outputs/misc_full_sae_eval/functional/misc_label_mapping/label_summary.json`：

| 标签 | 正样本数 | 占比 | 作用 |
|---|---:|---:|---|
| RE | 1358 | 0.219 | Reflection 总类 |
| RES | 516 | 0.083 | Simple Reflection 子类 |
| REC | 842 | 0.136 | Complex Reflection 子类 |
| QU | 1974 | 0.319 | Question 总类 |
| QUO | 1206 | 0.195 | Open Question 子类 |
| QUC | 768 | 0.124 | Closed Question 子类 |
| GI | 681 | 0.110 | Giving Information |
| SU | 222 | 0.036 | Support |
| AF | 349 | 0.056 | Affirm |
| OTHER | 1610 | 0.260 | 非核心 MISC 标签或无法归入核心集合的行为单元 |

## 3. 已获取的模型特征数据

### 3.1 SAE utterance-level features

| 文件 | 路径 | 形状/大小 | 作用 |
|---|---|---:|---|
| SAE 聚合 latent features | `outputs/misc_full_sae_eval/feature_store/utterance_features.pt` | `[6194, 32768]`，约 812 MB | 每条 utterance 在 SAE latent 空间中的表示，是 mapping、功能指标和候选因果的核心数据 |
| Llama 聚合 hidden activations | `outputs/misc_full_sae_eval/feature_store/utterance_activations.pt` | `[6194, 4096]`，约 101 MB | 每条 utterance 的模型层级句向量，用于保留原始 hidden representation |
| 特征元数据 | `outputs/misc_full_sae_eval/feature_store/feature_metadata.json` | 1 个 JSON | 记录特征形状、hook 点、聚合方式、标签名、batch size |

关键配置：

| 字段 | 数值 | 说明 |
|---|---|---|
| hook point | `blocks.19.hook_resid_post` | SAE 对齐的 Llama 层位置 |
| SAE latent 数量 | 32768 | 每条样本得到 32768 维 SAE feature |
| hidden size | 4096 | Llama hidden activation 维度 |
| 聚合方式 | `max` | token-level activation 聚合成 utterance-level feature |
| max sequence length | 128 | 单条输入最大 token 长度 |
| token-level latent 保存 | 关闭 | 避免全量 token 激活文件过大 |

这些特征的作用是：把文本样本转化成可统计分析的模型内部表征，从而研究“人工 MISC 标签”和“模型 SAE latent”之间的关系。

## 4. 已获取的结构指标

结构指标来自：

- `outputs/misc_full_sae_eval/metrics_structural.json`
- `outputs/misc_full_structural_rerun_20260503/metrics_structural.json`

它们衡量 SAE 对 Llama hidden states 的重构质量和稀疏程度。

| 指标 | 主运行数值 | 2026-05-03 复跑数值 | 作用 |
|---|---:|---:|---|
| `n_tokens` | 93636 | 93636 | 参与结构指标计算的 token 数 |
| `mse` | 7.4456 | 7.4907 | 重构误差，越低越好 |
| `cosine_similarity` | 0.8564 | 0.8563 | 原 hidden 与 SAE 重构 hidden 的方向相似度 |
| `ev_openmoss_legacy` | 0.5080 | 0.7635 | OpenMOSS legacy 口径 EV，是当前最接近官方论文口径的重构解释度 |
| `explained_variance` | -0.9751 | -0.9879 | 当前代码中另一种 EV 口径；与 legacy 口径不同，不宜混用 |
| `explained_variance_paper` | -0.8277 | -0.8396 | paper-style EV 变体；当前结果为负，需要在报告中解释口径差异 |
| `l0_mean` | 42.94 | 42.94 | 平均每个 token 激活的 latent 数，衡量稀疏性 |
| `dead_ratio` | 0.3231 | 0.3230 | 未激活或几乎不激活 latent 比例 |
| `ce_loss_delta` | 1.3528 | 1.3528 | SAE 重构对语言模型预测损失的影响 |
| `kl_divergence` | 2.4593 | 2.4593 | 原模型输出分布与重构输出分布的差异 |

使用建议：

- 论文或导师汇报中优先报告 `cosine_similarity`、`l0_mean`、`dead_ratio` 和 `ev_openmoss_legacy`。
- `ev_openmoss_legacy` 存在一次主运行为 `0.5080`、一次结构复跑为 `0.7635` 的差异，说明该指标对实现路径或口径较敏感，需要固定最终采用的运行版本。
- 其他 EV 变体为负，不能直接和官方论文中的 0.7 左右结果混为一谈。

## 5. 已获取的功能指标：RE vs NonRE

功能指标来自 `outputs/misc_full_sae_eval/metrics_functional.json`，主要回答：

> SAE latent 是否能区分 Reflection 与 Non-Reflection？

### 5.1 单 latent 显著性

| 指标 | 数值 | 作用 |
|---|---:|---|
| 总 latent 数 | 32768 | SAE feature 总维度 |
| FDR 显著 latent 数 | 3461 | 与 RE/NonRE 显著相关的 latent 数 |
| FDR alpha | 0.05 | 多重检验校正阈值 |
| Top RE latent | 29759 | 单 latent 层面最强 RE 候选之一 |
| Top latent Cohen's d | 0.847 | RE 与 NonRE 上激活差异的效应量 |
| Top latent AUC | 0.694 | 单 latent 区分 RE/NonRE 的能力 |

作用：说明不是所有 latent 都和 RE 相关，但有一批 latent 对 RE/NonRE 有显著区分能力。

### 5.2 稀疏 probe / dense probe

| 模型 | AUC | Accuracy | F1 | 作用 |
|---|---:|---:|---:|---|
| `sparse_probe_k1` | 0.6936 | 0.7840 | 0.5105 | 只用 1 个 latent 做 RE/NonRE 判别 |
| `sparse_probe_k5` | 0.7653 | 0.7565 | 0.5316 | 用 5 个 latent，看少量 latent 是否能组合表达 |
| `sparse_probe_k20` | 0.8245 | 0.7980 | 0.6002 | 用 20 个 latent，作为主要候选解释空间 |
| `dense_probe` | 0.8489 | 0.6921 | 0.5664 | 用更完整的 dense 表征作为性能上限参考 |
| `diffmean` | 0.7636 | 0.6380 | 0.3593 | 均值差方向的简单判别基线 |

使用建议：

- `sparse_probe_k20` 是当前比较适合汇报的功能性证据：它说明 RE 相关信息不是由单一 latent 完成，而是由一组 latent 共同表达。
- `dense_probe` AUC 更高，但解释性弱于 sparse probe。

## 6. 已获取的 MISC 多标签映射矩阵

多标签映射来自：

- `outputs/misc_full_sae_eval/functional/misc_label_mapping/latent_label_matrix.csv`
- `outputs/misc_full_sae_eval/functional/misc_label_mapping/label_summary.json`
- `outputs/misc_full_sae_eval/functional/misc_label_mapping/label_fragmentation.json`
- `outputs/misc_full_sae_eval/functional/misc_label_mapping/latent_overlap.json`

核心文件：

| 文件 | 大小/规模 | 作用 |
|---|---:|---|
| `latent_label_matrix.csv` | 约 52 MB | 每个 latent 与每个 MISC 标签的统计关联矩阵 |
| `label_indicator_matrix.csv` | 约 898 KB | 每条样本的标签指示矩阵 |
| `label_fragmentation.json` | 约 132 KB | 每个标签由多少 latent 分散表达 |
| `latent_overlap.json` | 约 17 KB | 每个 latent 同时关联多少标签 |
| `behavior_asymmetry.md` | 约 4 KB | 行为标签之间差异的文字摘要 |

作用：这是从“分类结果”进入“可解释性结构分析”的关键矩阵。它不说明某个 latent 等价于某个标签，而是说明 latent 与标签之间存在统计关联。

## 7. 已获取的 Mapping Structure 指标

Mapping Structure 结果来自 `outputs/misc_full_sae_eval/interpretability/mapping_structure`。

它回答导师文档中最核心的 R1 问题：

> MISC 标签空间与 SAE 表征空间是否是一一对应，还是存在结构化多对多映射？

### 7.1 Top20 候选空间整体指标

| 指标 | 数值 | 作用 |
|---|---:|---|
| 分析标签数 | 9 | 不含 `OTHER` 的核心 MISC 标签 |
| 每标签 TopK | 20 | 每个标签取 Top20 latent 作为解释候选 |
| latent-label 边数 | 180 | 9 个标签 × 20 个候选 |
| 去重 latent 数 | 136 | 180 条边实际涉及的唯一 latent |
| 单标签 latent | 103 | 只服务一个标签的候选 latent |
| 多标签 latent | 33 | 同时进入多个标签 Top20 的 latent |
| 多标签 latent 占比 | 24.3% | 支持“多对多映射”的核心证据 |

### 7.2 标签 fragmentation / 共享程度

| 标签 | Top20 shared ratio | Top latent | Top Cohen's d | Top AUC | 解释 |
|---|---:|---:|---:|---:|---|
| QU | 0.95 | 13430 | 2.584 | 0.925 | 高度共享，QU 与其他 question 类标签联系紧密 |
| QUO | 0.70 | 9959 | 1.762 | 0.800 | 与 QU 明显共享 |
| RE | 0.70 | 29759 | 0.847 | 0.694 | RE 与 REC 明显共享 |
| REC | 0.65 | 31133 | 0.965 | 0.611 | 与 RE 共享较强 |
| QUC | 0.35 | 21935 | 1.461 | 0.740 | 有一定共享，也有较多独立 latent |
| GI | 0.25 | 13430 | 0.698 | 0.682 | 与 QU 有部分重叠 |
| RES | 0.20 | 20808 | 0.701 | 0.626 | 相对更弱、更分散 |
| SU | 0.05 | 24760 | 1.063 | 0.571 | 较独立但样本少 |
| AF | 0.00 | 23464 | 2.237 | 0.800 | 最独立，呈 compact strong 模式 |

作用：说明不同 MISC 行为标签的表征结构并不相同。QU/QUO 更共享，AF 更独立，RE/REC 共享明显。

### 7.3 latent role taxonomy

| latent 角色 | 数量 | 占比 | 作用 |
|---|---:|---:|---|
| exclusive | 103 | 75.7% | 只进入一个标签 Top20，说明部分 latent 有较强标签专属性 |
| family_shared | 27 | 19.9% | 在同一标签家族内共享，如 RE/REC 或 QU/QUO |
| cross_family | 4 | 2.9% | 跨行为家族共享，如 question 与 information 的重叠 |
| global | 2 | 1.5% | 同时关联 5 个及以上标签，可能是更一般的语言/语境特征 |

作用：支持“模型内部表征比人工标签更连续、更混合”的结论。

### 7.4 父子标签层级一致性与层级去重 overlap

| 关系 | Jaccard | 解释 |
|---|---:|---|
| RE vs RES/REC child union | 0.277 | 父标签与子标签 union 有覆盖，但属于标签层级一致性 |
| QU vs QUO/QUC child union | 0.487 | 父标签与子标签 union 覆盖较高，但属于标签层级一致性 |
| QUO vs QUC | 0.053 | 同父子类之间相对分离 |
| RES vs REC | 0.000 | 同父子类在 Top20 中分离 |
| QUO vs GI | 0.143 | 最强 leaf-level 跨家族共享 |

作用：父子标签重叠只能作为 MISC 标注层级的一致性检查，不能作为独立的模型表征发现。论文中更稳妥的结论是：同父子类共享较低，跨家族共享较弱且集中。

## 8. 已获取的后续解释性分析指标

后续解释性结果来自 `outputs/misc_full_sae_eval/interpretability/followup_analysis`。

### 8.1 行为模式

| 指标 | 数值 | 作用 |
|---|---:|---|
| 分析标签数 | 9 | 核心 MISC 标签 |
| 每标签 Top latent | 20 | 与 Mapping Structure 一致 |
| `mixed_distributed` | 7 个标签 | 大多数行为由多个 latent 分散表达 |
| `compact_strong` | 1 个标签 | AF 表现为更集中、更独立 |
| `shared_distributed` | 1 个标签 | QU 表现为高度共享的分布式结构 |

### 8.2 latent case cards

| 指标 | 数值 | 作用 |
|---|---:|---|
| case cards 数 | 180 | 每个标签 Top20 latent 的案例卡片 |
| high purity candidate | 64 | 候选 latent 的 top examples 中目标标签较纯 |
| mixed but label relevant | 63 | 混合但仍有标签相关性 |
| low purity review required | 53 | 需要人工复核 |
| mean target purity | 0.546 | 平均目标标签纯度 |

作用：为后续人工语义审查提供样本级证据，而不只是统计数值。

### 8.3 会话质量差异

后续分析中还计算了 high / low quality 内部的 latent group score 差异，例如：

| 标签 | high-low 方向 | 作用 |
|---|---|---|
| RE | high 更高 | 高质量会话中反映类表征更强 |
| REC | high 更高 | 高质量会话中复杂反映更强 |
| AF | high 更高 | 高质量会话中肯定类行为更强 |
| QUC | low 更高 | 低质量会话中封闭问题相关表征更强 |

这些指标来自 `outputs/misc_full_sae_eval/interpretability/followup_analysis/followup_interpretability_metrics.json`，适合用于“行为标签与咨询质量之间的表征差异”讨论。

## 9. 已获取的因果验证候选组

候选因果结果来自 `outputs/misc_full_sae_eval/interpretability/causal_candidates`。

| 文件 | 作用 |
|---|---|
| `candidate_group_summary.csv` | 每个标签的 G1/G5/G10/G20 latent 组 |
| `causal_candidate_groups.json` | 机器可读的候选组 |
| `causal_candidate_metrics.json` | 候选组统计摘要 |
| `causal_candidate_report.md` | 候选组说明报告 |

当前候选组设置：

| 指标 | 数值 | 作用 |
|---|---:|---|
| 标签数 | 9 | 核心 MISC 标签 |
| 每标签候选数 | 20 | 从 Top20 mapping 中选择 |
| 分组 | G1/G5/G10/G20 | 用于后续干预实验的不同粒度 |

示例：

| 标签 | 模式 | G1 | G20 high-purity 数 | 作用 |
|---|---|---:|---:|---|
| RE | mixed_distributed | 19435 | 6 | 反映类候选干预组 |
| QU | shared_distributed | 13430 | 19 | 问题类候选干预组，纯度较高 |
| AF | compact_strong | 23464 | 7 | 肯定类候选干预组，相对独立 |

注意：这些是“因果验证候选”，还不是正式因果结论。真正因果结论需要运行 ablation / steering / selectivity 等干预实验。

## 10. 已获取的 Gemma layer probe 辅助指标

Gemma 层探针结果来自 `outputs/gemma3_misc_layer_probe`，模型为 `models/gemma-3-4b-pt`。这不是 SAE 主线，但可以作为“行为标签在普通 LLM hidden layers 中是否可线性读出”的辅助证据。

| 指标 | 数值 | 作用 |
|---|---:|---|
| 样本数 | 6194 | 与 MISC 主线一致 |
| 层数 | 34 | Gemma hidden layers |
| pooling | mean / last | 两种句向量聚合方式 |
| metric rows | 612 | 9 标签 × 34 层 × 2 pooling × 多折指标 |
| recognized labels | 9 / 9 | 9 个核心标签均超过 AUC 阈值 |

最佳层结果来自 `outputs/gemma3_misc_layer_probe/best_layers_by_label.csv`：

| 标签 | 最佳层 | Pooling | AUC | Balanced Acc. | 作用 |
|---|---:|---|---:|---:|---|
| QU | 10 | mean | 0.9745 | 0.9316 | Question 类最容易线性读出 |
| QUO | 15 | mean | 0.9541 | 0.8952 | 开放问题可读性强 |
| AF | 14 | mean | 0.9441 | 0.8053 | 肯定类可读性强，但 F1 受类别不平衡影响 |
| SU | 19 | mean | 0.9203 | 0.7871 | 支持类样本少但 AUC 高 |
| QUC | 17 | mean | 0.9211 | 0.8293 | 封闭问题可读性强 |
| RE | 18 | mean | 0.9099 | 0.8421 | 反映类可线性读出 |
| REC | 9 | mean | 0.9028 | 0.8036 | 复杂反映可线性读出 |
| GI | 10 | mean | 0.8730 | 0.7675 | 信息给予可读出 |
| RES | 15 | mean | 0.8192 | 0.7301 | 简单反映相对较弱 |

作用：

- 证明 MISC 行为标签在另一个模型的 hidden states 中也有可读信号。
- 可以作为 SAE 结果的外部辅助参照。
- 不能替代 SAE mapping，因为它没有 sparse latent 的可解释性结构。

## 11. 历史/旧阶段结果

以下结果已经获取，但不建议作为当前 MISC 全量主线的主要证据。

| 目录或文件 | 状态 | 原因 |
|---|---|---|
| `outputs/sae_eval_*` | 历史 SAE-RE 阶段 | 多数基于早期 RE/NonRE 或小样本流程 |
| `outputs/causal_validation_*` | 历史因果验证阶段 | 多数对应旧数据或旧候选 |
| `outputs/local_main_run_overnight` | 历史本地夜间实验 | 可作为调试记录，不是当前全量主线 |
| `outputs/sae_eval_bailian_qwen35plus/ai_judge_qwen35plus` | 历史 AI judge | 数据规模为 1598，且报告存在乱码，不建议直接用于当前 MISC 全量论文主结论 |
| `outputs/llamascope_sanity*` | 诊断/审计结果 | 用于检查 SAE 官方实现、EV 口径和重构路径 |

这些结果的主要价值是：帮助解释模型、SAE、指标口径和早期 pipeline 的演化，不宜直接混入当前 6194 条 MISC 全量实验结论。

## 12. 这些数据和指标分别能支持什么结论

| 研究结论 | 支撑数据/指标 | 当前证据强度 |
|---|---|---|
| MISC 标签在模型表征中有可读信号 | `metrics_functional.json`、Gemma layer probe AUC | 较强 |
| RE/NonRE 不是单个 latent 完成，而是多个 latent 共同表达 | sparse probe k1/k5/k20，Top20 mapping | 较强 |
| MISC 标签与 SAE latent 是多对多映射 | Top20 180 边、136 latent、33 multi-label latent | 较强 |
| 父子标签 overlap 符合标签体系但不能作为独立发现 | hierarchy-aware overlap：QU parent covered by children 0.95，RE parent covered by children 0.65 | 中等，作为层级一致性检查 |
| 同父子类与跨家族共享较弱 | QUO-QUC Jaccard 0.053，RES-REC 0.000，QUO-GI 0.143 | 较强，层级去重后更可信 |
| AF 更独立，QU 更共享 | fragmentation、role taxonomy、followup behavior pattern | 较强 |
| high / low quality 会话存在行为表征差异 | followup quality shift | 中等，需要控制混杂因素 |
| 某些 latent 是因果机制 | 当前不能证明 | 需要后续干预实验 |

## 13. 当前最应该用于论文/导师汇报的指标

优先级最高：

1. `dataset_summary.json`：说明数据规模和标签分布。
2. `utterance_features.pt` / `feature_metadata.json`：说明已经得到正式 SAE 表征矩阵。
3. `metrics_structural.json`：说明 SAE 在该数据上的重构质量和稀疏性。
4. `metrics_functional.json`：说明 RE/NonRE 在 SAE latent 中可读。
5. `latent_label_matrix.csv`：说明已经构造 `latent × MISC label` 矩阵。
6. `mapping_structure_metrics.json`：说明多对多映射结构。
7. `hierarchy_aware_overlap/` 与 `hierarchy_alignment.csv`：区分父子标签一致性、同父子类共享和跨家族 overlap，避免把父子包含关系误写成独立发现。
8. `followup_interpretability_metrics.json`：说明行为标签之间的表征差异和案例审查状态。
9. `causal_candidate_groups.json`：说明下一步因果验证要干预哪些 latent。

不建议作为主结论但可作为补充：

1. `outputs/gemma3_misc_layer_probe/*`：跨模型辅助证据。
2. `outputs/sae_eval_bailian_qwen35plus/ai_judge_qwen35plus/*`：旧阶段自动评审结果。
3. `outputs/llamascope_sanity*`：方法审计和调试证据。

## 14. 还缺什么

当前已经完成了“数据 -> SAE features -> 功能指标 -> mapping structure -> 候选解释”的主要链条，但还缺：

| 缺口 | 为什么重要 |
|---|---|
| 正式 causal intervention 结果 | 目前候选组不是因果结论，需要 ablation / steering / selectivity |
| 人工语义审查 | latent case cards 需要人工确认语义，避免把统计相关误写成语义等价 |
| 最终 EV 口径固定 | `ev_openmoss_legacy` 主运行和复跑差异较大，需要指定论文采用哪次、哪种口径 |
| 文档编码和历史结果清理 | 部分旧报告乱码或基于旧数据，容易干扰当前主线叙事 |
| 质量标签混杂控制 | high / low quality 差异可能受标签分布、会话长度、任务类型影响，需要谨慎解释 |

## 15. 总结

当前项目已经获取了三类核心证据：

1. **表征矩阵证据**：6194 条 MISC utterance 的 SAE latent features 和 Llama hidden activations。
2. **统计映射证据**：RE/NonRE 功能指标与 MISC 多标签 `latent × label` 矩阵。
3. **结构解释证据**：fragmentation、overlap、hierarchy alignment、behavior asymmetry 和 latent case cards。

这些证据足以支撑当前阶段的主要结论：

> MISC 人工行为标签并不等价于模型内部的单个 SAE latent；同一标签通常由多个 latent 共同表达，同一 latent 也可能参与多个标签，因此 MISC 标签空间与 SAE 表征空间更像是结构化的多对多映射。

但目前还不能直接声称：

> 某个 latent 因果决定了某种咨询行为。

要达到因果机制层面的结论，下一步需要基于 `outputs/misc_full_sae_eval/interpretability/causal_candidates` 中的 G1/G5/G10/G20 候选组，继续运行干预实验并做人工语义审查。

## 16. 2026-05-25 结果冻结审计结论

本节用于固定论文 Results 部分可采用的结果边界。审计依据包括当前 git 状态、`doc/` 和 `docs/` 文档、`outputs/` 结果目录以及已经删除但仍存在于 git 历史中的旧阶段结果。

### 16.1 需要固定为当前研究主线的结果

| 结果类别 | 固定位置 | 可写入论文的结论 |
|---|---|---|
| MISC 全量数据与特征矩阵 | `outputs/misc_full_sae_eval/dataset_summary.json`、`outputs/misc_full_sae_eval/feature_store/` | 当前主线基于 6194 条 MISC utterance，SAE feature 维度为 32768，hidden activation 维度为 4096，hook point 为 `blocks.19.hook_resid_post`。 |
| SAE 结构指标通过前提 | `outputs/misc_full_sae_eval/metrics_structural.json`、`outputs/misc_full_structural_rerun_20260503/metrics_structural.json` | cosine similarity 约为 0.856，`l0_mean` 约为 42.94，dead ratio 约为 0.323，说明该 SAE 表征在当前项目口径下可用于后续 mapping 与解释性分析。 |
| RE/NonRE 功能可读性 | `outputs/misc_full_sae_eval/metrics_functional.json` | 单 latent 的 RE/NonRE 识别能力有限，k1 AUC 为 0.6936；多 latent probe 明显增强，k20 AUC 为 0.8245，dense probe AUC 为 0.8489。论文中应写成“分布式可读信号”，不要写成“单个 latent 完成识别”。 |
| MISC label 与 latent 的统计映射 | `outputs/misc_full_sae_eval/functional/misc_label_mapping/` | 已形成 `latent × MISC label` 矩阵，可支持每个 MISC 标签对应哪些 SAE latent、强度如何、top examples 如何分布的分析。 |
| Top20 mapping structure | `outputs/misc_full_sae_eval/interpretability/mapping_structure/` | 9 个核心标签的 Top20 共有 180 条 label-latent 边，去重后 136 个 latent，其中 33 个是 multi-label latent，占 24.3%。这支持“MISC 标签与 SAE latent 是多对多结构映射”的主结论。 |
| latent role taxonomy | `outputs/misc_full_sae_eval/interpretability/mapping_structure/latent_role_summary.csv` | Top20 latent 中 exclusive 103 个、family_shared 27 个、cross_family 4 个、global 2 个。可以写入 Results 作为结构化解释证据。 |
| MISC 层级感知 overlap | `outputs/misc_full_sae_eval/interpretability/mapping_structure/hierarchy_aware_overlap/` | 父子 overlap 只作为标签层级一致性检查；层级去重后，QUO-QUC Jaccard 为 0.053，RES-REC 为 0.000，最强 leaf-level 跨家族对 QUO-GI 为 0.143。 |
| 后续解释性分析 | `outputs/misc_full_sae_eval/interpretability/followup_analysis/` | 可写成行为标签表征模式：QU 更 shared-distributed，AF 更 compact-strong，多数标签是 mixed-distributed。该部分适合放在 Results 后半或 Discussion。 |
| 因果验证候选组 | `outputs/misc_full_sae_eval/interpretability/causal_candidates/` | 只能固定为“后续干预实验候选组”，不能写成因果结论。 |
| Gemma3-4B 辅助选层 | `outputs/gemma3_re_nonre_layer_probe/best_layers_by_label.csv` | 使用平衡 RE/NonRE 数据集筛层时，RE 最优层为 layer 10、mean pooling、AUC 0.9755。该结果适合服务后续 Gemma 消融实验，不应替代 SAE 主线结果。 |

### 16.2 SAE 结构指标口径的固定建议

当前存在两份结构指标：

| 来源 | n_tokens | cosine_similarity | ev_openmoss_legacy | l0_mean | dead_ratio |
|---|---:|---:|---:|---:|---:|
| `outputs/misc_full_sae_eval` | 93636 | 0.8564 | 0.5080 | 42.9396 | 0.3231 |
| `outputs/misc_full_structural_rerun_20260503` | 93636 | 0.8563 | 0.7635 | 42.9372 | 0.3230 |

冻结原则：

1. 论文 Results 中的 mapping、functional、hierarchy、followup 和 causal candidates 均以 `outputs/misc_full_sae_eval` 为主线来源。
2. `outputs/misc_full_structural_rerun_20260503` 只作为结构指标复核来源，用来说明 cosine、稀疏度和 dead ratio 稳定；不要用它替换主线 mapping 结果。
3. `ev_openmoss_legacy` 主运行和复跑差异较大，因此论文中不宜把 EV 写成唯一质量门槛。更稳妥的写法是报告 cosine similarity、`l0_mean`、dead ratio，并说明 EV 采用 legacy 兼容口径且存在复跑差异。

### 16.3 已经过时或只作历史参考的结果

| 结果位置 | 当前状态 | 处理建议 |
|---|---|---|
| `sae_eval_full/` | 工作区中已删除，git 历史中仍可见 | 旧 RE/NonRE 1598 样本阶段结果，不应混入当前 6194 条 MISC 全量 Results。 |
| `causal_validation_full/` | 工作区中已删除，git 历史中仍可见 | 对应旧候选组和旧流程，不能作为当前 MISC 因果结论。 |
| `outputs/sae_eval_*` | 历史 SAE-RE 或调试结果 | 仅用于方法演化说明，不作为论文主结果。 |
| `outputs/causal_validation_*` | 历史因果验证结果 | 候选 latent 与当前 MISC Top20 候选不一致，不作为当前论文主结果。 |
| `outputs/llamascope_sanity*`、`outputs/sae_parity*` | 诊断/指标审计 | 可用于解释 SAE 官方实现、EV 口径和重构路径，不作为实验发现。 |
| `outputs/gemma3_misc_layer_probe_smoke*`、`outputs/gemma3_misc_layer_probe_token_probe` | smoke 或测试性输出 | 不作为论文结果。 |
| `outputs/gemma3_misc_layer_probe` | 全量 MISC layer probe | 可作跨模型辅助证据；但因为 MISC 标签正负不均衡，正式 Gemma 消融选层优先采用 `gemma3_re_nonre_layer_probe`。 |

### 16.4 论文 Results 推荐写法

可以固定为 Results 的核心发现：

1. 在通过 SAE 结构质量检查的前提下，MISC 全量数据的 SAE latent 表征可用于行为标签解释性分析。
2. RE/NonRE 行为信号不是单一 latent 表达，而是多个 latent 的分布式组合；k20 sparse probe 明显优于 k1。
3. MISC 标签与 SAE latent 呈现多对多映射，而不是一对一语义对齐。
4. 父子标签 overlap 不能作为独立发现；层级去重后，同父子类共享较低，跨家族共享较弱且集中。
5. AF 更接近紧凑强信号标签，QU 更接近共享分布式标签，其他标签多为 mixed-distributed。
6. 当前 causal candidates 已经固定，但只能作为下一阶段 ablation/steering 的实验入口。

不应写成 Results 主结论的内容：

1. 不应声称某个 latent 因果决定某个 MISC 行为。
2. 不应声称每个 MISC 标签都有唯一对应 latent。
3. 不应把旧 `sae_eval_full` 或旧 `causal_validation_full` 结果与当前 MISC 全量主线混写。
4. 不应把 RES 写成强结构恢复标签；当前证据更适合写成弱恢复或负例。
5. 不应把 Gemma layer probe 当成 SAE 可解释性结果；它是后续消融实验的选层依据。

### 16.5 当前冻结决策

截至 2026-05-25，论文 Results 部分建议固定采用：

1. 主线结果目录：`outputs/misc_full_sae_eval`
2. 结构指标复核目录：`outputs/misc_full_structural_rerun_20260503`
3. Gemma 消融选层辅助目录：`outputs/gemma3_re_nonre_layer_probe`
4. 过时结果边界：`sae_eval_full/`、`causal_validation_full/`、`outputs/sae_eval_*`、`outputs/causal_validation_*` 不进入当前主 Results。

这个冻结决策可以被后续正式 causal intervention 结果更新；在此之前，当前论文主线应以“统计关联、结构映射、候选机制”为边界，不越界写成因果机制。

## 17. 2026-05-29 Latent 空间搜索 v2 固定结果

本节用于固定重构后的 MISC SAE latent 空间搜索结果。它与第 7 节的旧 Top20 mapping structure 互补，但口径不同：Top20 现在只作为人工可审查候选窗口，正式统计结论以 thresholded set、weighted metrics 和 K-sensitivity 为主。

### 17.1 新主结果目录

| 项目 | 位置 | 用途 |
|---|---|---|
| v2 主报告 | `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/latent_space_search_report.md` | 论文 Results 中解释 latent 空间搜索的主报告 |
| v2 汇总 JSON | `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/latent_space_search_summary.json` | 固定输入、阈值、输出文件、核心计数 |
| Top20 候选窗口 | `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/top20_candidate_set_v2.csv` | 仅用于人工审查、热力图和候选浏览 |
| 正式稳定 latent 集 | `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/thresholded_latent_sets_v2.csv` | 支撑“某标签是否有稳定 SAE latent 表征”的正式证据 |
| Fragmentation | `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/fragmentation_v2.csv` | 衡量每个标签需要多少 stable latents |
| Thresholded overlap | `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/overlap_thresholded_v2.csv` | 衡量稳定 latent 集合之间的共享；包含 raw 与 specific-only 两种 Jaccard |
| Weighted overlap | `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/overlap_weighted_v2.csv` | 衡量连续弱信号上的标签相似性 |
| Polysemanticity / role taxonomy | `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/polysemanticity_v2.csv`、`latent_role_assignments_v2.csv` | 判断 latent 是 label-specific、sibling/shared、cross-family 还是 generalized |
| K-sensitivity | `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/k_sensitivity_summary_v2.csv` | 检查结构结论是否依赖单一 K 值 |
| Semantic review candidates | `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/semantic_review_candidates_v2.csv`、`semantic_top_utterances_v2.csv` | 为后续人工语义复核导出候选 latent 与 top utterances |
| Baseline comparison | `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/baseline_lightweight_v2.csv` | 接入 Step6 的 SAE/PCA/raw hidden 对照，并补充 random/base-rate 轻量 baseline |

### 17.2 固定阈值与解释口径

| 指标 | 阈值 / 口径 | 解释 |
|---|---|---|
| `directional_auc` | `>= 0.70` | latent 激活值对标签的排序识别能力达到可用阈值 |
| `abs_cohens_d` | `>= 0.50` | 正负样本激活均值差达到中等效应量 |
| `significant_fdr` | `True` | 多重比较校正后仍显著 |
| `precision_at_50` | `>= prevalence + 0.10` | 正向 support latent 的高激活样本中目标标签富集，且至少高于基线 10 个百分点 |
| `negative_boundary` | 不要求高激活 P@50 | 负向 latent 只解释边界或排斥信号，不作为正向语义证据 |
| Top20 | `candidate_budget_k=20` | 仅表示人工可审查预算，不是正式 stable latent 数 |
| `specific_jaccard` | 排除 generalized latent 后再算 Jaccard | 用于判断标签对特异性共享，避免泛化 latent 抬高 raw overlap |

### 17.3 主要固定数值

| 指标 | 数值 |
|---|---:|
| 输入 `latent × label` 行数 | 294912 |
| 核心标签数 | 9 |
| Top20 candidate edges | 180 |
| Formal thresholded edges | 28 |
| positive support edges | 26 |
| negative boundary edges | 2 |
| no-stable placeholder rows | 3 |
| label-specific stable latents | 16 |
| generalized stable latents | 1 |

### 17.4 每个标签的稳定 latent 表征

| 标签 | 范围 | Stable latents | Effective fragmentation | 分类 | 当前解释 |
|---|---|---:|---:|---|---|
| RE | parent consistency only | 0 | 0.000 | no stable latents | 父标签只做一致性检查；严格阈值下不作为独立稳定概念结论 |
| RES | leaf | 0 | 0.000 | no stable latents | 当前 SAE 空间中未找到满足 formal threshold 的稳定 RES latent |
| REC | leaf | 5 | 4.906 | compact | 复杂反映存在较紧凑的稳定 latent 集合 |
| QU | parent consistency only | 8 | 7.604 | moderate | 父标签可用于一致性参考，不作为独立 overlap 发现 |
| QUO | leaf | 9 | 8.513 | distributed | 开放式问题呈更分散的 latent 表征 |
| QUC | leaf | 2 | 1.983 | compact | 封闭式问题在严格阈值下较紧凑 |
| GI | leaf | 0 | 0.000 | no stable latents | 信息给予标签有连续弱信号，但未达到 formal stable threshold |
| SU | leaf | 1 | 1.000 | compact | 严格阈值下主要表现为 boundary latent |
| AF | leaf | 3 | 1.898 | compact | 肯定/支持类有少量稳定 latent，其中包含正向和边界信号 |

### 17.5 Overlap 与 polysemanticity 的固定解释

raw thresholded overlap 中最高的 leaf pairs 为：

| 标签对 | Raw Jaccard | Specific Jaccard | 共享 latent | 解释 |
|---|---:|---:|---|---|
| QUC-SU | 0.500 | 0.000 | 13430 | 共享完全由 generalized latent 贡献，不写成标签对特异性共享 |
| SU-AF | 0.333 | 0.000 | 13430 | 共享完全由 generalized latent 贡献 |
| QUC-AF | 0.250 | 0.000 | 13430 | 共享完全由 generalized latent 贡献 |
| QUO-SU | 0.111 | 0.000 | 13430 | 共享完全由 generalized latent 贡献 |
| QUO-QUC | 0.100 | 0.000 | 13430 | 同父类 raw overlap 存在，但 strict specific-only overlap 为 0 |

因此，正式写作时应区分三层结论：

1. **Top20 candidate 结论**：可以说明哪些 latent 值得人工审查，但不作为正式稳定集合。
2. **Thresholded stable-set 结论**：可以说明哪些标签有稳定 latent 表征，以及碎片化程度。
3. **Specific-only overlap 结论**：当前严格阈值下，leaf-pair 的特异性共享很弱；raw overlap 主要由 generalized latent `13430` 驱动。

weighted overlap 的最高 leaf pairs 为 `RES-GI`、`QUO-QUC`、`RES-QUC`、`QUC-GI`、`REC-GI`。这些结果说明标签之间存在连续弱相似性，但它们不是 formal thresholded shared latent 结论，适合写成补充分析或稳健性参考。

### 17.6 与旧 Top20 口径的关系

第 7 节中固定的 Top20 mapping structure 仍可保留，但应调整写法：

1. Top20 的 `180` 条边、`136` 个 unique latent、`33` 个 multi-label latent 可以作为“候选空间结构”结果。
2. v2 的 `thresholded_latent_sets_v2.csv` 才是“稳定 latent 集合”的正式结果。
3. 如果 Top20 与 thresholded set 给出不同强度，应优先采用 v2 formal threshold 结果。
4. 论文中不要把“进入 Top20”写成“已经稳定表示该标签”；应写成“进入人工审查候选窗口”。

### 17.7 当前可写入论文的更新结论

可以写入 Results：

1. MISC 行为标签在 SAE latent 空间中呈现不均匀结构：`REC/QUC/AF/SU` 较紧凑，`QUO` 更分散，`RES/GI` 在严格阈值下未恢复出稳定 latent 集。
2. Top20 candidate 分析显示候选空间存在多标签共享，但 v2 strict threshold 进一步表明，强证据共享主要集中在 generalized latent，而不是稳定的标签对特异性共享。
3. `specific_jaccard` 结果提醒：不能把 raw overlap 直接解释为两个 MISC 标签在 SAE 空间中拥有稳定共享机制。
4. SAE 相比 PCA/raw hidden 的优势不只体现在预测性能，而在于更低的 effective-n fraction 与更清晰的 latent role taxonomy；但分类 F1 本身不应作为可解释性主结论。

不应写入 Results：

1. 不应声称 Top20 latent 就是正式机制集合。
2. 不应声称 `QUC-SU`、`SU-AF` 等 raw high-overlap 标签对存在标签特异共享机制；这些共享目前主要来自 generalized latent `13430`。
3. 不应把 `RES/GI` 写成已经有稳定 support latent 的标签；当前只可说存在 weighted 弱信号或候选审查线索。
4. 不应把 negative boundary latent 写成正向语义 latent。
