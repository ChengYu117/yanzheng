# 日志

## 2026-05-04 MISC 可解释性工作简明汇报

- 新增老师版简明报告：`doc/MISC可解释性工作简明汇报.md`。
- 报告基于已有全量结果整理，不重新跑模型：
  - `outputs/misc_full_sae_eval/dataset_summary.json`
  - `outputs/misc_full_sae_eval/interpretability/mapping_structure/mapping_structure_metrics.json`
  - `outputs/misc_full_sae_eval/interpretability/followup_analysis/followup_interpretability_metrics.json`
- 核心结论：
  - `6194` 条 MISC utterance 已完成 SAE 表征映射。
  - Top20 候选空间包含 `180` 条 latent-label 边、`136` 个去重 latent，其中 `33` 个为多标签共享 latent，占比 `24.3%`。
  - 结果支持“人工标签与 SAE latent 不是一一对应，而是结构化多对多关系”。
  - 报告明确限制：当前是结构/相关性分析，不是因果证明；单个 latent 不能直接等价于某个 MISC 标签。

## 2026-04-30

- 本地复跑导师要求的 MISC 可解释性分析三段流程：
  - Mapping Structure：`run_misc_mapping_structure_analysis.py`
  - 后续行为差异与 latent case：`run_misc_interpretability_analysis.py`
  - 因果候选组导出：`run_misc_causal_candidate_export.py`
- 复跑输出目录：`outputs/misc_full_sae_eval/interpretability/local_rerun_20260430_202407`。
- 核心结果：
  - FDR 显著 latent-label 边：`44308`
  - 至少关联一个标签的 latent：`13052`
  - 单标签 latent：`2147`
  - 多标签 latent：`10905`，占比 `0.836`
  - 生成 latent case cards：`45`，其中 `20` 个为 high-purity candidate。
- 研究判断：
  - 支持导师 R1：MISC 标签与 SAE latent 是结构化多对多映射，不是一一对应。
  - 支持 R2：`QU/QUO` 更紧凑，`RE/REC` 更分布式共享，`SU/AF/RES` 更像 negative-boundary 标签。
  - 已导出 RE、RES、REC、QU、QUO、QUC、GI、SU、AF 的 G1/G5/G10/G20 因果候选组。
- AI 专家代理评审接口检查：
  - `run_ai_re_judge.py --help` 通过。
  - `test_ai_re_judge_smoke.py` 通过，`6 passed, 0 failed`。
  - 本次未正式调用外部 API，AI judge 结果不计入当前研究结论。
- 本次复跑相关测试：
  - `test_mapping_structure_analysis.py` 通过，`2 passed, 0 failed`。
  - `test_behavior_interpretability.py` 通过，`2 passed, 0 failed`。
  - `test_causal_candidate_export.py` 通过，`2 passed, 0 failed`。
- 详细报告：`doc/MISC可解释性工作本地复跑综合分析报告.md`。
- 云端因果验证脚本改为全量 MISC 数据集口径：
  - `deploy/gce/common.sh` 和 `deploy/gce/env.example` 新增 `CAUSAL_DATA_DIR=${DATA_DIR}`、`ALLOW_LEGACY_CAUSAL_DATA=0`。
  - `deploy/gce/run_causal.sh` 默认使用 `CAUSAL_DATA_DIR=data/mi_quality_counseling_misc`，优先读取 `${CAUSAL_CANDIDATE_OUTPUT_DIR}/label_candidates/${CAUSAL_LABEL}_candidate_latents.csv`。
  - 默认拒绝 `data/mi_re`、`data/cactus`、`derived/re_nonre` 等旧数据路径，除非显式设置 `ALLOW_LEGACY_CAUSAL_DATA=1`。
  - `run_causal.sh` 不再接受追加 `--data-dir`，统一通过 `CAUSAL_DATA_DIR` 控制，避免正式运行误切到旧平衡集。
  - 验证：`bash -n deploy/gce/common.sh deploy/gce/run_causal.sh deploy/gce/run_full_pipeline.sh deploy/gce/start_tmux_job.sh` 通过；`test_deploy_smoke.py` 通过，`5 passed, 0 failed`。

## 2026-04-27

- 完成一次 MISC latent-label 矩阵生成 smoke：
  - 复用 `outputs/structural_smoke_misc/feature_store/utterance_features.pt`，对前 32 条 MISC 样本生成 `latent_label_matrix.csv`。
  - 输出目录：`outputs/structural_smoke_misc/functional/misc_label_mapping`。
  - 结果文档：`doc/MISC矩阵生成结果评测报告.md`。
  - 工程结论：矩阵生成、标签分布、碎片化、latent overlap、TopK Jaccard 和 top examples 均成功落盘。
  - 科研结论：本次只是 32 条小样本 smoke，`SU` 因正例不足被跳过，`AF` 出现 6 个 FDR 显著 latent，但不作为正式结论；正式结论需跑全量 6194 条样本。
- 完成 MISC 全量数据驱动的 SAE 主流程重构：
  - 新增统一数据读取接口，默认读取 `data/mi_quality_counseling_misc/misc_annotations/*/*.jsonl`。
  - 主流程以 `unit_text` 为统一样本单位，生成 `dataset_summary.json`、`records.jsonl`、`label_matrix.csv` 和 `feature_store/`。
  - `run_sae_evaluation.py` 新增 `--data-format`、`--label-mode`、`--max-samples`、`--save-features`、`--save-token-topk` 等参数。
  - 功能指标拆为 `functional/re_binary/` 和 `functional/misc_label_mapping/`，并保留根目录 `candidate_latents.csv` 兼容因果验证。
  - 因果验证和 Stage 2 激活抽取均切换为统一 dataset loader。
  - 云端脚本默认 `DATA_DIR=data/mi_quality_counseling_misc`，本地与云端教程已补充新版命令。
- 验证：
  - `python -m py_compile ...` 通过。
  - `conda run -n qwen-env-py311 python test_dataset_loader_smoke.py` 通过。
  - `conda run -n qwen-env-py311 python test_misc_label_mapping_smoke.py` 通过。
  - `conda run -n qwen-env-py311 python test_pipeline_smoke.py` 通过。
  - `conda run -n qwen-env-py311 python test_causal_smoke.py` 通过。
  - `conda run -n qwen-env-py311 python test_stage2_smoke.py` 通过。
  - `conda run -n qwen-env-py311 python test_deploy_smoke.py` 通过。
  - `bash -n deploy/gce/run_full_eval.sh` 通过。

## 2026-04-22

- 完成对 SAE 结构指标口径的进一步核查，并补跑了基于本地缓存的 `400` 条论文代理集正式实验。
- 结果表明：按 OpenMOSS 官方 evaluator 的 `legacy EV` 口径，论文代理集达到 `0.7131`，已经落在此前预期的 `0.6 ~ 0.7` 区间附近。
- 对应结果文件：
  - `outputs/llamascope_sanity_400_metric_audit_cached/metrics_structural.json`
  - `outputs/llamascope_sanity_400_metric_audit_cached/comparison_vs_mire.json`
- 同时需要保留的说明：
  - 官方 `legacy EV` 达到约 `0.7131`
  - 但 `ev_openmoss_aligned` 仍为负值（约 `-0.7107`）
  - 说明“论文中的 0.6~0.7 数值”更可能对应官方 `legacy EV`，不能直接等同于当前 raw-space 主 EV 已经达标
- 项目指标使用策略调整：
  - 暂时不再把负值的 `ev_openmoss_aligned / ev_centered_legacy / ev_llamascope_paper` 作为主判据
  - 当前默认主指标切换为 `ev_openmoss_legacy`
  - 负值 EV 保留为辅助诊断字段，仅用于后续排查和对照

## 2026-04-22 PAI 实验跑通步骤

- 目标：
  - 将当前项目打包为 Docker 镜像
  - 上传到阿里云 ACR
  - 在 PAI-EAS 中以 `ScalableJobService` 形态部署
  - 通过 HTTP 接口触发 `llamascope_sanity` / `sae_parity` / `variant_audit` / `sae_eval`

- 第 1 步：准备模型与输出挂载目录
  - 将基础模型放到 OSS 挂载路径：
    - `/mnt/pai/models/Llama-3.1-8B`
  - 预留输出与缓存路径：
    - `/mnt/pai/outputs`
    - `/mnt/pai/cache/hf`
    - `/mnt/pai/data`

- 第 2 步：构建并推送镜像到 ACR
  - 配置环境变量：
    - `ACR_REGISTRY`
    - `ACR_NAMESPACE`
    - `ACR_REPOSITORY`
    - `IMAGE_TAG`
  - PowerShell 执行：
    - `powershell -ExecutionPolicy Bypass -File deploy/pai/build_and_push.ps1`
  - Bash 执行：
    - `bash deploy/pai/build_and_push.sh`
  - 关键文件：
    - `deploy/pai/Dockerfile`
    - `deploy/pai/.dockerignore`
    - `deploy/pai/build_and_push.ps1`
    - `deploy/pai/build_and_push.sh`

- 第 3 步：准备 EAS 部署模板
  - 打开：
    - `deploy/pai/eas_service.json`
  - 替换占位符：
    - `__REGION_ID__`
    - `__OSS_BUCKET__`
    - `__IMAGE_URI__`
  - 当前模板约定：
    - 前端服务监听 `8000`
    - `MODEL_DIR=/mnt/pai/models/Llama-3.1-8B`
    - `OUTPUT_ROOT=/mnt/pai/outputs`
    - `HF_HOME=/mnt/pai/cache/hf`

- 第 4 步：在 PAI-EAS 控制台部署
  - 选择“自定义镜像部署”
  - 使用 `ScalableJobService`
  - 挂载 OSS 数据集到：
    - `/mnt/pai/models`
    - `/mnt/pai/outputs`
    - `/mnt/pai/cache/hf`
    - `/mnt/pai/data`
  - 启动服务后记录 EAS 访问地址

- 第 5 步：验证服务健康状态
  - 调用：
    - `GET /healthz`
  - 示例：
    - `curl http://<eas-endpoint>/healthz`
  - 预期：
    - 返回 `status = ok`

- 第 6 步：提交最小实验任务
  - 先跑小规模 `llamascope_sanity`
  - 示例：
    - `POST /jobs/run`
    - 请求体：
      - `{"job_type":"llamascope_sanity","output_subdir":"run_20260422","args":{"max_docs":8,"batch_size":4,"max_seq_len":128,"checkpoint_topk_semantics":"hard"}}`
  - 查询状态：
    - `GET /jobs/<job_id>`
  - 查询产物：
    - `GET /jobs/<job_id>/artifacts`

- 第 7 步：验证关键输出
  - `llamascope_sanity` 预期产出：
    - `metrics_structural.json`
    - `comparison_vs_mire.json`
    - `metric_provenance.json`
  - `sae_parity` 预期产出：
    - `parity_report.json`
  - `variant_audit` 预期产出：
    - `variant_evidence_matrix.json`
    - `variant_decision.json`

- 第 8 步：扩大到正式实验
  - 小规模任务成功后，再把 `max_docs` 调到正式值，例如 `400`
  - 推荐先跑：
    - `llamascope_sanity`
    - `sae_parity`
  - 最后再跑完整 `sae_eval`

- 当前实现说明：
  - 当前 v1 服务已经能在 EAS 上通过 HTTP 触发整套实验
  - 任务执行器是在服务容器内以子进程调用现有 runner
  - 当前还没有直接接入阿里云专有队列 SDK，后续如需更深度的 EAS 作业调度，可再演进

## 2026-04-22 本地实验流程说明补充

- 新增文档：
  - `doc/本地实验运行说明.md`
- 作用：
  - 说明如何在本地机器上，从环境准备开始，逐步跑通整个实验流程
  - 不涉及云服务器、Docker、ACR、PAI-EAS
- 推荐顺序：
  1. `run_sae_checkpoint_parity.py`
  2. `run_llamascope_distribution_sanity.py`
  3. `run_llamascope_variant_audit.py`
  4. `run_sae_evaluation.py`
- 当前本地口径说明：
  - 与论文数值对齐时，主看 `ev_openmoss_legacy`
  - 负值 EV 只保留为辅助诊断指标
- 文档补充：
  - 已在 `doc/本地实验运行说明.md` 中新增“夜间一键顺序跑完整流程”命令
  - 可直接依次执行 `sae_parity -> llamascope_sanity -> variant_audit -> sae_eval`
  - 默认输出到 `outputs/local_full_run_overnight/`

## 2026-04-22 主实验流程口径调整

- 当前主实验流程调整为：
  - 只跑本地数据集 `data/mi_re`
  - 默认主流程执行：
    - `run_sae_evaluation.py`
    - `causal/run_experiment.py`
- 论文代理数据集相关脚本现在统一降级为**调试专用**：
  - `run_sae_checkpoint_parity.py`
  - `run_llamascope_distribution_sanity.py`
  - `run_llamascope_variant_audit.py`
- 已更新 `doc/本地实验运行说明.md`：
  - 增加“当前默认主实验流程（只跑本地数据集）”
  - 增加新的夜间命令，串联本地主实验 + 因果实验
  - 修正夜间命令中的关键参数：
    - 显式使用 `--data-dir data/mi_re`
    - 显式使用 `--checkpoint-topk-semantics hard`

## 2026-04-25 local_main_run_overnight 结果分析

- 新增结果分析文档：`doc/local_main_run_overnight结果分析报告.md`
- 对应结果目录：`outputs/local_main_run_overnight`
- 运行状态：因果验证阶段已完整完成，`run_status.json` 显示 `status=completed`，总耗时约 `12h25m55s`。
- 简要结论：
  - SAE 功能指标显示 RE/NonRE 差异信号较强：`2187 / 32768` 个 latent 通过 FDR 显著性筛选，Top-20 sparse probe 达到 `AUC=0.9158`，dense probe 达到 `AUC=0.9677`。
  - 因果 necessity 结果较有力：G20 ablation 使 RE logit 下降约 `-1.258`，而 Bottom20 和 Random20 基本接近零，说明候选 latent 组不是随机噪声。
  - Sufficiency/steering 有效但选择性不足：Top 组 steering 会提升 RE logit，同时也会提升 NonRE logit，因此更适合表述为“相关语义/表达方向有效”，暂不宜宣称为高度选择性的 RE 因果开关。
  - 需要修复的关键问题：`metrics_structural.json` 中 `ev_openmoss_legacy` 仍为 `None`，当前 full structural 路径不能直接用于和论文 legacy EV 数值对齐。

## 2026-04-25 修复 full structural 的 `ev_openmoss_legacy` 落盘

- 问题定位：
  - 测试/诊断脚本中已经实现了 `OfficialMetricsAccumulator`，并通过 `apply_full_structural_metrics(...)` 把 `metrics/explained_variance_legacy` 映射为 `ev_openmoss_legacy`。
  - 主流程 `run_sae_evaluation.py --full-structural` 只启用了 raw/normalized 两个 `OnlineStructuralAccumulator`，没有启用 official accumulator，导致 `ev_openmoss_legacy` 在主结果里为 `None`。
- 修复内容：
  - `run_sae_evaluation.py` 已引入 `OfficialMetricsAccumulator`。
  - `--full-structural` 路径现在会同时累计 raw、normalized 和 official OpenMOSS metrics。
  - 合并结果时改为复用诊断路径的 `apply_full_structural_metrics(...)`，新生成的 `metrics_structural.json` 应包含 `official_metrics`、`ev_openmoss_aligned`、`ev_openmoss_legacy`、`l0_official` 等字段。
- 验证情况：
  - `python -m py_compile run_sae_evaluation.py src\nlp_re_base\diagnostics.py src\nlp_re_base\eval_structural.py src\nlp_re_base\activations.py` 通过。
  - `conda run -n qwen-env-py311 python run_sae_evaluation.py --help` 通过，说明主入口导入链正常。
  - `conda run -n qwen-env-py311 python -m unittest test_sae_diagnostics_smoke.TestDiagnosticsHelpers.test_apply_full_structural_metrics_prefers_official_ev_field` 通过，说明 official legacy EV 字段映射逻辑仍正确。

## 2026-04-27 实现 MISC 多标签 `Latent × Label` 表征映射

- 目标：
  - 根据导师 `project summary.docx` 中“标注空间与表征空间存在结构化、多对多错配”的指导，将项目从 RE/NonRE 二分类扩展到完整 MISC 行为标签映射分析。
  - 生成可用于论文叙事的 `Latent × Label` association matrix、标签碎片化、latent 重叠和行为不对称分析。
- 新增实现：
  - `src/nlp_re_base/misc_label_mapping.py`
    - 读取 `data/mi_quality_counseling_misc/misc_annotations` 中的完整 MISC 标注。
    - 自动构造层级标签，例如 `RE/RES/REC/QU/QUO/QUC/GI/SU/AF/OTHER`。
    - 对每个 label 与每个 latent 计算 `Cohen's d`、`AUC`、`p-value`、`BH-FDR`、`precision@k`。
    - 输出 `label_fragmentation.json`、`latent_overlap.json`、`label_topk_jaccard.json` 和 `behavior_asymmetry.md`。
  - `run_misc_label_mapping.py`
    - 支持直接加载 Llama + OpenMOSS SAE 抽取全部 MISC utterance features。
    - 支持通过 `--features-path` 复用已抽取 features 进行快速重分析。
  - `test_misc_label_mapping_smoke.py`
    - 使用合成 MISC 数据和合成 features 验证模块调用与 CLI 调用。
  - `src/nlp_re_base/__init__.py`
    - 导出 `load_misc_annotation_records` 和 `run_misc_label_mapping`。
- 文档补充：
  - `doc/MISC数据集SAE表征映射执行大纲.md`
    - 记录正式运行命令、复用 features 命令、主要输出文件和测试入口。
- 主要输出文件约定：
  - `latent_label_matrix.csv`
  - `label_summary.json`
  - `label_fragmentation.json`
  - `latent_overlap.json`
  - `label_topk_jaccard.json`
  - `behavior_asymmetry.md`
  - `top_latents_by_label/`
  - `top_examples_by_label/`
- 验证情况：
  - `conda run -n qwen-env-py311 python test_misc_label_mapping_smoke.py` 通过，`2 passed, 0 failed`。
  - `conda run -n qwen-env-py311 python test_pipeline_smoke.py` 通过，`12 passed, 0 failed`。
  - `conda run -n qwen-env-py311 python run_misc_label_mapping.py --help` 通过。
  - `conda run -n qwen-env-py311 python -m compileall run_misc_label_mapping.py src\nlp_re_base\misc_label_mapping.py test_misc_label_mapping_smoke.py` 通过。
## 2026-04-29 MISC 全量 SAE 可用性分析

- 新增分析文档：`doc/MISC全量SAE可用性分析报告.md`
- 对应结果目录：`outputs/misc_full_sae_eval`
- 数据口径：全量 MISC 行为单元，共 `6194` 条样本。
- 简要结论：
  - 当前 SAE 在该数据集上具备研究可用性，适合用于候选 latent 发现、RE/NonRE 功能筛选、MISC 多标签映射和后续因果验证入口。
  - 结构保真度的 `ev_openmoss_legacy` 采用 2026-05-03 结构复跑结果；主运行中的旧 legacy EV 记录已移除。`cosine_similarity=0.8564` 说明有一定重构能力；但 `ce_loss_delta=1.3528`、`kl_divergence=2.4593`，说明还不能作为高保真激活替代。
  - RE/NonRE 功能指标显示 `3461` 个 latent 通过 FDR 显著性筛选，Top-20 sparse probe 达到 `AUC=0.8245`，说明 RE 信号在 SAE 空间中可被捕获。
  - MISC 多标签矩阵规模为 `32768 × 10 = 327680` 行，`QU/QUO/QUC` 信号最强，`RE/REC/RES` 存在稳定但更分散的表征。
  - 标签碎片化与 latent overlap 明显：`13052` 个 latent 至少关联一个标签，其中 `10905` 个关联多个标签，支持“标注空间与表征空间存在多对多错配”的研究叙事。
- 可用性判断：
  - 可以支持“发现 MISC/RE 相关候选表征子空间”。
  - 暂不宜宣称“单一 latent 即 RE 概念”或“已经发现高保真、单义的行为机制”。

## 2026-04-29 Mapping Structure 分析阶段实现

- 新增工程入口：
  - `run_misc_mapping_structure_analysis.py`
  - `src/nlp_re_base/mapping_structure.py`
  - `test_mapping_structure_analysis.py`
- 新增报告：`doc/MISC Mapping Structure分析报告.md`
- 分析目标：
  - 将 `latent × MISC label` 矩阵转化为导师文档 R1 的正式证据。
  - 证明 MISC 标签空间与 SAE latent 表征空间不是一一对应，而是结构化多对多映射。
- 新增分析内容：
  - label fragmentation 排名。
  - latent overlap / polysemanticity 分布。
  - top-k Jaccard、full-significant Jaccard、Cohen's d 相关。
  - `RE -> RES, REC` 与 `QU -> QUO, QUC` 层级一致性。
  - exclusive / family-shared / cross-family / global latent role taxonomy。
- 默认输出目录：
  - `outputs/misc_full_sae_eval/interpretability/mapping_structure`
- 验证情况：
  - `.venv` 环境完成 `py_compile` 语法检查。
  - 已通过 `qwen-env-py311` 执行新增合成测试，`2 passed`。
  - 已在全量 `outputs/misc_full_sae_eval/functional/misc_label_mapping` 上跑通正式 Mapping Structure 分析。
  - 关键断言通过：`significant_latent_label_edges=44308`、`latents_with_any_significant_label=13052`、`single_label_latents=2147`、`multi_label_latents=10905`。
  - 正式输出目录：`outputs/misc_full_sae_eval/interpretability/mapping_structure`

## 2026-04-30 MISC 后续可解释性与因果候选组导出

- 新增工程入口：
  - `run_misc_interpretability_analysis.py`
  - `src/nlp_re_base/behavior_interpretability.py`
  - `test_behavior_interpretability.py`
  - `run_misc_causal_candidate_export.py`
  - `src/nlp_re_base/causal_candidates.py`
  - `test_causal_candidate_export.py`
- 新增报告：
  - `doc/MISC后续可解释性阶段分析报告.md`
  - `doc/MISC因果验证候选组说明.md`
- 新增正式输出目录：
  - `outputs/misc_full_sae_eval/interpretability/followup_analysis`
  - `outputs/misc_full_sae_eval/interpretability/causal_candidates`
- 后续可解释性阶段完成内容：
  - 生成行为差异摘要、质量标签激活差异、标签家族不对称摘要和 `45` 张 latent case card。
  - 行为模式分为 `compact_strong=2`、`negative_boundary=3`、`shared_distributed=4`。
  - `QU/QUO` 呈现更紧凑的强信号；`RE/REC/QUC/GI` 更像共享分布式信号；`AF/SU/RES` 更像边界或负向分离信号。
  - case card 结果：`20` 个 high-purity candidate、`14` 个 mixed-but-label-relevant、`11` 个需要人工复核，平均 top-example target purity 约 `0.604`。
- 因果候选组导出完成内容：
  - 对 `RE/RES/REC/QU/QUO/QUC/GI/SU/AF` 生成 G1/G5/G10/G20 候选组、bottom control 和 random control。
  - 候选排序综合 `Cohen's d`、`directional AUC`、`precision@50`、case card 纯度和行为差异 top-positive 排名。
  - `RE` 的 G20 已导出为后续 RE/NonRE 因果验证入口；`QU/QUO` 适合作为更紧凑标签的对照因果实验；`AF/SU/RES` 后续解释时应优先看 necessity，谨慎声明 sufficiency。
- 验证情况：
  - `qwen-env-py311` 下 `py_compile` 通过：`src\nlp_re_base\behavior_interpretability.py`、`run_misc_interpretability_analysis.py`、`test_behavior_interpretability.py`、`src\nlp_re_base\causal_candidates.py`、`run_misc_causal_candidate_export.py`、`test_causal_candidate_export.py`。
  - `qwen-env-py311 python test_mapping_structure_analysis.py` 通过，`2 passed, 0 failed`。
  - `qwen-env-py311 python test_behavior_interpretability.py` 通过，`2 passed, 0 failed`。
  - `qwen-env-py311 python test_causal_candidate_export.py` 通过，`2 passed, 0 failed`。
  - `qwen-env-py311 python test_causal_smoke.py` 通过，`22 tests OK`。
- 当前科研判断：
  - 已形成从 `latent × label` 结构证据，到行为差异解释，再到后续因果验证候选组的完整衔接。
  - 这些结果仍是候选解释与候选因果入口，不能直接写成“单个 latent 等价于某个 MISC 标签”或“已经完成因果证明”。

## 2026-04-30 MISC 数据集代码对接说明

- 新增文档：`doc/MISC数据集代码对接说明.md`
- 文档说明范围：
  - `data/mi_quality_counseling_misc/misc_annotations/*/*.jsonl` 如何被读取为样本。
  - 每条 `unit_text` 如何作为一个独立 SAE 推理样本。
  - `predicted_code/predicted_subcode` 如何映射为 `RE/RES/REC/QU/QUO/QUC/GI/SU/AF/OTHER` 多标签。
  - `label_re` 二分类标签如何生成。
  - 文本如何进入 tokenizer、Llama layer-19 hook 和 OpenMOSS SAE。
  - token-level latent 如何通过 `max` 聚合为 `utterance_features.pt`。
- 关键说明：
  - 当前主流程不拼接完整会话上下文，只输入单条 MISC 行为单元。
  - `quality_label` 只表示 high/low 会话质量，不是行为标签。
  - `rationale` 不输入模型，只用于解释审查。

## 2026-05-01 Top20 可解释性流程复跑与科研可接受性评估

- 复跑命令：
  - `python run_misc_mapping_structure_analysis.py --analysis-top-k 20 --top-k 20`
  - `python run_misc_interpretability_analysis.py --top-latents-per-label 20`
  - `python run_misc_causal_candidate_export.py --candidate-top-k 20`
- 新增报告：
  - `doc/MISC Top20可解释性流程复跑科研可接受性评估报告.md`
- 核心结果：
  - 正式解释对象为每个核心 MISC 标签的 Top20 latent，共 `180` 条候选边。
  - 去重后 `136` 个 unique latent，其中 `33` 个为多标签共享 latent，多标签共享比例为 `0.243`。
  - `QU-QUO` 与 `RE-REC` 是最清晰的标签共享结构；`AF` 最独立；`RES` 当前解释质量较弱。
  - 共生成 `180` 张 case cards，其中 `64` 张为 high-purity candidate，`63` 张为 mixed-but-label-relevant，`53` 张需要人工复核。
- 科研判断：
  - 当前结果可以作为 Mapping Structure、行为不对称性和因果候选池的正式研究证据。
  - 不宜直接声称单个 latent 等价于某个 MISC 标签，也不能替代后续因果干预和人工语义复核。
## 2026-05-20 旧阶段代码清理

- 清理目标：
  - 将仓库代码从早期 cactus / Stage2 / 基础推理 demo 口径收束到当前 MISC 全量 SAE 可解释性主线。
- 已删除：
  - `build_cactus_dataset.py`
  - `verify_cactus_pipeline.py`
  - `run_stage2_activation_extraction.py`
  - `src/nlp_re_base/stage2_activation_extraction.py`
  - `test_stage2_smoke.py`
  - `run_inference.py`
  - `src/nlp_re_base/infer.py`
  - `inspect_dataset.py`
  - `data/cactus/` 下旧 cactus 小样本与说明文件
- 已同步：
  - `src/nlp_re_base/__init__.py` 移除 Stage2 lazy export。
  - `package_project.py` 不再打包 cactus、Stage2 和旧推理 demo。
  - `deploy/pai/Dockerfile` 移除旧推理入口复制。
  - `test_deploy_smoke.py` 更新发布包断言。
  - `causal/run_experiment.py` 默认数据目录改为 `data/mi_quality_counseling_misc`。
  - `README.md` 重写为当前 MISC SAE 可解释性主线说明。
- 新增说明：
  - `doc/旧阶段代码清理说明.md`
- 验证：
  - `conda run -n qwen-env-py311 python -m py_compile ...` 通过。
  - `conda run -n qwen-env-py311 python test_deploy_smoke.py` 通过，`5 tests OK`。
  - `conda run -n qwen-env-py311 python test_dataset_loader_smoke.py` 通过，`3 passed`。
  - `conda run -n qwen-env-py311 python test_misc_label_mapping_smoke.py` 通过，`2 passed`。
  - `conda run -n qwen-env-py311 python test_mapping_structure_analysis.py` 通过，`2 passed`。
  - `conda run -n qwen-env-py311 python test_behavior_interpretability.py` 通过，`2 passed`。
  - `conda run -n qwen-env-py311 python test_causal_candidate_export.py` 通过，`2 passed`。

## 2026-05-23 旧入口与 PAI 部署残留继续清理

- 按当前 MISC 全量 SAE 主线继续删除不再维护的入口和结果残留：
  - `run_misc_label_mapping.py` 顶层离线 CLI。
  - `sae_eval_full/` 与 `causal_validation_full/` 早期结果目录。
  - `deploy/pai/`、`src/nlp_re_base/pai/`、`test_pai_deploy_smoke.py` 与 `doc/PAI-EAS部署运行教程.md`。
- 保留：
  - `src/nlp_re_base/misc_label_mapping.py`，因为 `run_sae_evaluation.py` 主流程仍调用其中的 `run_misc_label_mapping()`。
- 同步：
  - `package_project.py` 不再打包 `run_misc_label_mapping.py`。
  - `test_misc_label_mapping_smoke.py` 只保留模块级测试，不再测试已删除 CLI。
  - `test_deploy_smoke.py` 不再断言发布包包含 `run_misc_label_mapping.py`。
  - `pyproject.toml` 与 `requirements.txt` 移除 PAI 服务入口专用的 FastAPI/Uvicorn 依赖。
  - `docs/PROJECT_DOSSIER.md` 将 PAI-EAS 状态改为已从当前主线清理。
- 验证：
  - `conda run -n qwen-env-py311 python -m py_compile package_project.py run_sae_evaluation.py src\nlp_re_base\misc_label_mapping.py test_misc_label_mapping_smoke.py test_deploy_smoke.py` 通过。
  - `conda run -n qwen-env-py311 python test_misc_label_mapping_smoke.py` 通过，`1 passed`。
  - `conda run -n qwen-env-py311 python test_deploy_smoke.py` 通过，`5 tests OK`。

## 2026-05-29 MISC SAE latent 空间搜索 v2 重构、复跑与结果固定

- 目标：
  - 根据 `doc/Top-20 SAE Latents Evaluation.pdf` 的方法建议，重构 latent 搜索流程。
  - 将旧的 Top20 口径降级为人工可审查候选窗口，不再把 Top20 本身当作正式统计判定阈值。
  - 正式结论改由 thresholded set、weighted metrics、K-sensitivity、semantic review candidates 和 baseline comparison 共同支撑。
- 新增代码：
  - `src/nlp_re_base/latent_space_search_v2.py`
  - `run_misc_latent_space_search_v2.py`
  - `test_latent_space_search_v2_smoke.py`
- 正式输入：
  - `outputs/misc_full_sae_eval/functional/misc_label_mapping/latent_label_matrix.csv`
  - `outputs/misc_full_sae_eval/feature_store/utterance_features.pt`
  - `outputs/misc_full_sae_eval/label_matrix.csv`
  - `outputs/misc_full_sae_eval/records.jsonl`
  - `outputs/misc_full_sae_eval/interpretability/baseline_comparison_step6/table4_baseline_comparison.csv`
- 正式输出目录：
  - `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/`
- 生成的核心结果文件：
  - `latent_label_association_v2.csv`
  - `top20_candidate_set_v2.csv`
  - `thresholded_latent_sets_v2.csv`
  - `weighted_latent_label_matrix_v2.csv`
  - `fragmentation_v2.csv`
  - `overlap_thresholded_v2.csv`
  - `overlap_weighted_v2.csv`
  - `polysemanticity_v2.csv`
  - `latent_role_assignments_v2.csv`
  - `hierarchy_recovery_v2.csv`
  - `k_sensitivity_summary_v2.csv`
  - `semantic_review_candidates_v2.csv`
  - `semantic_top_utterances_v2.csv`
  - `baseline_lightweight_v2.csv`
  - `latent_space_search_summary.json`
  - `latent_space_search_report.md`
- 生成图：
  - `figures/latent_label_heatmap_top20.png`
  - `figures/latent_label_heatmap_thresholded.png`
  - `figures/weighted_overlap_heatmap.png`
  - `figures/k_sensitivity_curves.png`
  - `figures/fragmentation_effective_n_bar.png`
  - `figures/polysemanticity_distribution.png`
- 正式阈值：
  - `directional_auc >= 0.70`
  - `abs_cohens_d >= 0.50`
  - `significant_fdr == True`
  - 正向 support latent 额外要求 `precision_at_50 >= prevalence + 0.10`
  - 负向 latent 只作为 `negative_boundary`，不把高激活 P@50 当作正向语义证据。
- 运行命令：
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe -m py_compile run_misc_latent_space_search_v2.py src\nlp_re_base\latent_space_search_v2.py test_latent_space_search_v2_smoke.py`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe test_latent_space_search_v2_smoke.py`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe run_misc_latent_space_search_v2.py`
- 验证结果：
  - 编译检查通过。
  - `test_latent_space_search_v2_smoke.py` 通过。
  - 兼容性 smoke 通过：
    - `test_mapping_structure_analysis.py`：`2 passed`
    - `test_step6_baseline_comparison_smoke.py`：`1 passed`
  - 全量 v2 流程完成，`n_rows=294912`、`n_labels=9`、Top20 candidate edges=`180`、formal thresholded edges=`28`。
  - 所有必需 CSV / JSON / Markdown / PNG 文件生成。
  - `jaccard`、`specific_jaccard`、`weighted_jaccard` 均在 `[0,1]`。
  - `effective_fragmentation <= thresholded_latent_count` 检查通过。
  - K-sensitivity 覆盖 `K=5,10,20,50,100`。
- 审计后修正：
  - 初次复核发现 raw thresholded overlap 中若干高 Jaccard 都由同一个 generalized latent `13430` 贡献。
  - 为避免把泛化 latent 造成的共享误写成标签对特异性共享，已在 `overlap_thresholded_v2.csv` 中新增：
    - `generalized_shared_latents`
    - `specific_intersection`
    - `specific_union`
    - `specific_jaccard`
    - `shared_specific_latents`
  - 报告中新增 `Specific-only thresholded leaf overlaps`，明确 raw overlap 与 label-pair-specific overlap 的区别。
- 当前结果摘要：
  - 严格 formal threshold 下，`REC`、`QU`、`QUO`、`QUC`、`SU`、`AF` 找到 stable latents；`RE`、`RES`、`GI` 未达到稳定 latent 阈值。
  - `REC`：5 个 stable latents，effective fragmentation=4.906，compact。
  - `QUO`：9 个 stable latents，effective fragmentation=8.513，distributed。
  - `QUC`：2 个 stable latents，effective fragmentation=1.983，compact。
  - `SU`：1 个 boundary latent，compact。
  - `AF`：3 个 stable latents，其中 2 个 positive support、1 个 boundary，effective fragmentation=1.898，compact。
  - `polysemanticity_v2.csv` 中共有 `16` 个 label-specific latent 和 `1` 个 generalized latent；generalized latent 为 `13430`。
- 解释性结论边界：
  - Top20 仍可用于人工审查和热力图展示，但正式 Results 不应写成“Top20 就是稳定 latent 集合”。
  - raw thresholded overlap 可提示 generalized latent 造成的共享，但 label-pair-specific overlap 应优先看 `specific_jaccard`。
  - 本次 formal threshold 下 leaf-pair 的 specific thresholded overlap 为 0，说明强阈值支持的共享主要来自 generalized latent，而不是稳定的标签对特异性共享。
  - weighted overlap 仍可作为连续弱信号参考；其最高 leaf pair 包括 `RES-GI`、`QUO-QUC`、`RES-QUC`、`QUC-GI`、`REC-GI`。
  - 父标签 `RE/QU` 只做 parent-child consistency，不进入独立 overlap 发现。

## 2026-05-29 MISC 最小充分 SAE latent 子空间 v2

- 目标：
  - 回答“每个 MISC 标签到底需要多少个 SAE latents 才能被很好表征”。
  - 不再把 `TopK=1/3/5/10/20` 作为结论，而是自动搜索最小充分 latent 子空间 `S*`。
  - 该实验是 probe-space 的预测充分性分析，不写成因果充分性。
- 新增代码：
  - `src/nlp_re_base/minimal_sufficient_subspace_v2.py`
  - `run_misc_minimal_sufficient_subspace_v2.py`
  - `test_minimal_sufficient_subspace_v2_smoke.py`
- 正式输入：
  - `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/latent_label_association_v2.csv`
  - `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/thresholded_latent_sets_v2.csv`
  - `outputs/misc_full_sae_eval/feature_store/utterance_features.pt`
  - `outputs/misc_full_sae_eval/label_matrix.csv`
- 正式输出目录：
  - `outputs/misc_full_sae_eval/interpretability/minimal_sufficient_subspace_v2/`
- 生成结果文件：
  - `minimal_sufficient_summary_v2.csv`
  - `minimal_sufficient_selected_latents_v2.csv`
  - `minimal_sufficient_fold_results_v2.csv`
  - `minimal_sufficient_selection_steps_v2.csv`
  - `minimal_sufficient_redundancy_audit_v2.csv`
  - `minimal_sufficient_curves_v2.csv`
  - `candidate_pool_v2.csv`
  - `minimal_sufficient_summary.json`
  - `minimal_sufficient_subspace_report.md`
- 生成图：
  - `figures/minimal_k_by_label.png`
  - `figures/full_vs_minimal_auc.png`
  - `figures/selection_stability_heatmap.png`
  - `figures/redundancy_by_label.png`
  - `figures/sufficiency_curves_by_label.png`
- 方法口径：
  - 候选池为 formal stable latents 加 `association_rank <= 100` 的备份候选。
  - 每个标签使用 `5-fold Stratified CV`。
  - 每折训练 full-candidate logistic probe。
  - 在验证折上使用 full probe 的线性贡献做 additive greedy selection，寻找最小子集。
  - 最小充分 K 需要同时满足：
    - `AUC >= 0.70`
    - `AUC >= full_candidate_auc - 0.02`
    - `AUPRC >= full_candidate_auprc - 0.03`
    - `Precision@50 lift >= full_candidate_precision_lift_at_50 - 0.05`
- 验证命令：
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe -m py_compile run_misc_minimal_sufficient_subspace_v2.py src\nlp_re_base\minimal_sufficient_subspace_v2.py test_minimal_sufficient_subspace_v2_smoke.py src\nlp_re_base\__init__.py`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe test_minimal_sufficient_subspace_v2_smoke.py`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe run_misc_minimal_sufficient_subspace_v2.py`
- 回归测试：
  - `test_latent_space_search_v2_smoke.py` 通过。
  - `test_mapping_structure_analysis.py` 通过。
  - `test_step6_baseline_comparison_smoke.py` 通过。
- 全量结果摘要：
  - 9 个核心标签在 Top100 候选池下 full-candidate probe 均达到 recoverable。
  - 父标签：
    - `RE`：full AUC=0.896，median minimal K=22，只作为 parent consistency。
    - `QU`：full AUC=0.975，median minimal K=6，只作为 parent consistency。
  - leaf 标签：
    - `RES`：full AUC=0.863，median minimal K=17，distributed。
    - `REC`：full AUC=0.907，median minimal K=13，distributed。
    - `QUO`：full AUC=0.961，median minimal K=10，distributed。
    - `QUC`：full AUC=0.927，median minimal K=15，distributed。
    - `GI`：full AUC=0.851，median minimal K=19，distributed。
    - `SU`：full AUC=0.876，median minimal K=14，distributed。
    - `AF`：full AUC=0.929，median minimal K=11，distributed。
- 解释性结论边界：
  - `latent_space_search_v2` 回答的是“哪些 latent 达到严格 stable-edge 阈值”。
  - `minimal_sufficient_subspace_v2` 回答的是“在 stable+Top100 候选池里，需要多少 latent 才能接近 full-candidate 预测性能”。
  - 因此二者结论并不矛盾：严格 stable latents 可以很少，但要达到接近 full probe 的预测充分性，需要更多弱/互补 latent。
  - 当前结果支持“各 MISC leaf 标签不是由少数 1-3 个 latent 稳定充分表示，而是需要约 10-19 个 latent 的分布式子空间”。
  - 这些 `S*` 可作为后续 ablation / steering 的优先候选组，但当前不能直接写成因果机制集合。

## 2026-05-30 MISC 标签结构问题结论固定

- 固定目标：
  - 将以下问题记录为当前阶段实验结果：
    - 哪些标签 compact？
    - 哪些标签 fragmented？
    - 哪些标签对共享表征？
    - shared latents 是否符合 MISC family structure？
    - 哪些标签有清晰关联 latents？
    - 哪些标签关联较弱？
    - 不同标签是否对应不同 latent patterns？
- 依据结果：
  - `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/fragmentation_v2.csv`
  - `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/overlap_thresholded_v2.csv`
  - `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/overlap_weighted_v2.csv`
  - `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/hierarchy_recovery_v2.csv`
  - `outputs/misc_full_sae_eval/interpretability/latent_space_search_v2/polysemanticity_v2.csv`
  - `outputs/misc_full_sae_eval/interpretability/minimal_sufficient_subspace_v2/minimal_sufficient_summary_v2.csv`
- 口径区分：
  - `strict stable-edge` 口径回答“哪些 latent 与标签有强、清晰、单边统计关联”。
  - `minimal sufficient subspace` 口径回答“多个强/弱 latent 组合时，最少多少 latent 才能接近完整候选池预测表现”。
  - 两个口径必须同时保留，不能把 strict compact 误写成真正的少量 latent 预测充分。
- compact / fragmented 结论：
  - strict stable-edge 下，`REC`、`QUC`、`SU`、`AF` 可视为 compact 或少量 stable-edge 标签。
  - 但 minimal sufficient subspace 下，所有 leaf 标签均为 distributed：
    - `QUO=10`
    - `AF=11`
    - `REC=13`
    - `SU=14`
    - `QUC=15`
    - `RES=17`
    - `GI=19`
  - 因此正式写作应以“各 leaf 标签预测充分子空间均为分布式”为主结论。
- shared representation 结论：
  - raw thresholded overlap 中最高共享对包括 `QUC-SU`、`SU-AF`、`QUC-AF`、`QUO-SU`、`QUO-QUC`。
  - 这些 raw overlap 主要由 generalized latent `13430` 驱动。
  - 排除 generalized latent 后，leaf-pair `specific_jaccard=0`，说明当前 strict threshold 下没有稳定的标签对特异性 shared latent。
  - weighted overlap 显示连续弱共享：`RES-GI`、`QUO-QUC`、`RES-QUC`、`QUC-GI`、`REC-GI`、`RES-REC`、`SU-AF`。
- MISC family structure 结论：
  - `QU/QUO/QUC` 问题类有一定 family recovery：
    - `QU` vs `QUO/QUC` child union Jaccard=0.636。
    - `QU` covered by children=0.875。
    - `children` covered by `QU`=0.700。
    - `QUO-QUC` weighted overlap=0.186。
  - `RE/RES/REC` 反映类恢复弱：
    - strict parent-child overlap 为 0。
    - `RES-REC` strict overlap=0，weighted overlap=0.140。
  - 因此可写成“问题类层级恢复较好，反映类层级恢复较弱；共享结构只部分符合 MISC family structure”。
- clear / weak association 结论：
  - 清晰关联 latent 较多的标签：`QUO`、`REC`、`QUC`、`AF`；父标签 `QU` 作为 consistency 也较强。
  - 关联较弱或更分散的标签：`RES`、`GI`、`SU`；父标签 `RE` 不作为独立清晰恢复结论。
  - `RES/GI/SU` 不是没有 SAE 信号，而是缺少少量强 stable latent；Top100 子空间仍可预测恢复，说明信号更分散。
- latent pattern 结论：
  - `REC/QUO/QUC/AF`：strict-clear + distributed sufficient。
  - `RES/GI`：weak-stable + distributed sufficient。
  - `SU/AF`：包含 boundary latent，不应全部写成正向语义 latent。
  - latent `13430`：generalized cross-family latent，连接 `AF/QUC/QUO/SU`，不能写成标签对特异共享机制。
- 当前解决程度：
  - compact / fragmented、clear / weak associations、different latent patterns 已经可以较好回答。
  - shared latents 与 family structure 可以回答为“部分符合”，但不能写成完整恢复 MISC 层级。
  - 当前结论足以支撑 Results 中的结构性结论，但不足以支撑因果机制结论。

## 2026-05-30 代码入口、使用方式与复现说明固定

- 固定目标：
  - 将最新可解释性实验的代码构建内容、运行入口、默认输入、输出文件和解释边界写入项目文档。
  - 让后续复跑时可以明确区分 `latent_space_search_v2` 与 `minimal_sufficient_subspace_v2` 两条口径。
- 已更新文档：
  - `README.md`
  - `doc/已获取数据与指标清单.md`
- README 新增内容：
  - `run_misc_latent_space_search_v2.py` 的用途、默认输入、关键输出和 strict stable-edge 阈值。
  - `run_misc_minimal_sufficient_subspace_v2.py` 的用途、候选池、5-fold CV、greedy selection、最小充分 K 判定条件和输出目录。
  - `src/nlp_re_base/latent_space_search_v2.py`、`src/nlp_re_base/minimal_sufficient_subspace_v2.py` 与对应 smoke tests 的代码结构说明。
- `doc/已获取数据与指标清单.md` 新增第 20 节：
  - 固定 latent 空间搜索 v2 的代码入口与产物表。
  - 固定最小充分 latent 子空间 v2 的代码入口、核心模块、`__init__` 导出、默认运行命令、核心产物和解释边界。
  - 明确 `Top20` 只作为人工审查候选预算，`thresholded_latent_sets_v2.csv` 是 strict stable-edge 主口径。
  - 明确最小充分子空间是 probe-space predictive sufficiency，不等价于 causal sufficiency。
- 当前代码侧固定结论：
  - `latent_space_search_v2` 负责回答“哪些 latent 达到严格 stable-edge 阈值”。
  - `minimal_sufficient_subspace_v2` 负责回答“在 stable+Top100 候选池内，需要多少 latent 才能接近 full-candidate probe 表现”。
  - 两个实验互补，不应互相替代。

## 2026-05-30 Gemma3-4B + GemmaScope Layer-18 SAE 对照实验

- 实验目标：
  - 构建独立的 Gemma3-4B 可解释性主线，用 GemmaScope SAE 复刻当前 Llama + OpenMOSS SAE 的 MISC 分析闭环。
  - 检查当前结构性发现是否只是 Llama 或某个 SAE checkpoint 的特殊性。
  - 本轮主层固定为 Gemma 0-based `layer_idx=18`，因为全量 MISC layer probe 中父标签 `RE` 的最佳层为 layer 18；`QU` 的最佳层为 layer 10，作为补充说明保留。
- SAE checkpoint：
  - `google/gemma-scope-2-4b-pt/resid_post_all/layer_18_width_16k_l0_small`
  - hook 校验为 `model.layers.18.output`
  - width=`16384`
- 新增代码：
  - `src/nlp_re_base/gemma_scope_sae.py`
  - `src/nlp_re_base/gemma_scope_pipeline.py`
  - `run_gemma_scope_sae_evaluation.py`
  - `run_cross_model_sae_comparison.py`
  - `test_gemma_scope_sae_smoke.py`
  - `test_gemma_scope_pipeline_smoke.py`
  - `test_cross_model_sae_comparison_smoke.py`
- 新增文档：
  - `doc/GemmaScope对照实验说明.md`
  - `README.md` 新增 Gemma3-4B + GemmaScope Layer-18 对照实验运行说明。
  - `doc/已获取数据与指标清单.md` 新增第 21 节，固定 GemmaScope 代码入口、产物和结果摘要。
- 实现口径：
  - Gemma model 默认优先读取本地 `models/gemma-3-4b-pt`，不存在时使用 Hugging Face `google/gemma-3-4b-pt`。
  - GemmaScope SAE 轻量 loader 读取 `config.json` 与 `params.safetensors`。
  - JumpReLU 公式固定为：
    - `pre = x @ w_enc + b_enc`
    - `latents = pre * (pre > threshold)`
    - `recon = latents @ w_dec + b_dec`
  - token-level SAE latents 使用 `max` pooling 得到 utterance-level features。
  - `OTHER` 不进入主结论标签。
- 运行中发现并修正的问题：
  - 初始 full run 使用 `float16` SAE 前向时，结构指标出现非有限值。
  - 已将主入口默认 `--sae-dtype` 改为 `float32`，并在结构指标累积器中跳过非有限 token，记录 `n_invalid_tokens_skipped`。
  - float32 重跑后 full structural metrics 全部 finite，`n_invalid_tokens_skipped=0`。
- 验证命令：
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe -m py_compile run_gemma_scope_sae_evaluation.py run_cross_model_sae_comparison.py src\nlp_re_base\gemma_scope_sae.py src\nlp_re_base\gemma_scope_pipeline.py test_gemma_scope_sae_smoke.py test_gemma_scope_pipeline_smoke.py test_cross_model_sae_comparison_smoke.py`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe test_gemma_scope_sae_smoke.py`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe test_gemma_scope_pipeline_smoke.py`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe test_cross_model_sae_comparison_smoke.py`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe test_latent_space_search_v2_smoke.py`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe test_minimal_sufficient_subspace_v2_smoke.py`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe test_step6_baseline_comparison_smoke.py`
- 小样本 smoke：
  - `run_gemma_scope_sae_evaluation.py --layer-idx 18 --limit-records 64 --min-positive 1 --min-negative 1 --batch-size 2 --output-dir outputs/gemma3_l18_gemmascope_sae_eval_smoke`
  - 生成 feature store、label matrix、structural metrics 和 latent-label matrix。
  - smoke structural metrics 已 finite：explained variance=0.999120，cosine=0.998431，L0 mean=22.798。
- 全量 GemmaScope 主评估：
  - 输出目录：`outputs/gemma3_l18_gemmascope_sae_eval/`
  - `utterance_features.pt` shape=`[6194, 16384]`
  - `utterance_activations.pt` shape=`[6194, 2560]`
  - structural metrics：
    - valid tokens=97055
    - invalid tokens skipped=0
    - explained variance=0.999013
    - cosine similarity=0.998405
    - MSE=2145.545
    - MAE=27.728
    - L0 mean=22.825
    - L0 std=16.872
- 全量 `latent_space_search_v2` 结果：
  - Top20 candidate edges=180。
  - strict thresholded edges=19。
  - 无 stable latent 的标签：`RES`、`GI`。
  - stable latent 数：
    - `RE=1`
    - `RES=0`
    - `REC=5`
    - `QU=4`
    - `QUO=5`
    - `QUC=1`
    - `GI=0`
    - `SU=1`
    - `AF=2`
- 全量 `minimal_sufficient_subspace_v2` 结果：
  - `RE` parent：full AUC=0.906，median K=18。
  - `RES`：full AUC=0.870，median K=18，distributed。
  - `REC`：full AUC=0.918，median K=15，distributed。
  - `QU` parent：full AUC=0.974，median K=9。
  - `QUO`：full AUC=0.961，median K=14，distributed。
  - `QUC`：full AUC=0.919，median K=10，distributed。
  - `GI`：full AUC=0.857，median K=20，distributed。
  - `SU`：full AUC=0.921，median K=17，distributed。
  - `AF`：full AUC=0.944，median K=7，moderate。
- Gemma Step6 baseline comparison：
  - SAE：mean label AUC=0.768，macro probe AUC=0.917，macro F1=0.594。
  - PCA：mean label AUC=0.762，macro probe AUC=0.931，macro F1=0.664。
  - raw hidden：mean label AUC=0.751，macro probe AUC=0.896，macro F1=0.547。
  - 解释：PCA 在部分预测指标上更高，但 SAE 提供原生稀疏 latent 单位，因此仍作为可解释性主表征；PCA/raw hidden 是 predictive control。
- 跨模型对照结论：
  - `REC/QUO/QUC/AF` 在 Llama 与 Gemma 中均表现为相对清晰的 MISC 关联标签。
  - `RES/GI` 在两模型中都缺少强 stable single-latent，但 Top100 子空间仍可预测恢复，说明其表征更分散。
  - `SU` 在两模型中均只有少量 stable edge，但达到 full-candidate 表现需要更多 latent。
  - Gemma 中 `AF` 的最小充分子空间更紧，median K=7；其他 leaf 标签大多仍为 distributed。
  - 当前跨模型证据支持“主要 MISC 结构不是 Llama/OpenMOSS 单一 checkpoint 的偶然产物”，但仍不等同于因果机制证明。

## 2026-05-30 GemmaScope 对照实验自动验收闭环

- 新增验收入口：
  - `run_gemma_scope_validation.py`
- 验收目标：
  - 不重新跑昂贵的 Gemma forward，而是对已经完成的 full pipeline artifacts 做完整性和数值合法性验收。
  - 若验收失败，用非零退出码暴露问题，便于自动迭代。
- 验收覆盖：
  - `records.jsonl`、`label_matrix.csv`、feature store、structural metrics、latent-label matrix、latent-space v2、minimal sufficient v2、Step6 baseline、cross-model comparison 等 21 个核心 artifact。
  - `utterance_features.pt` shape=`[6194, 16384]`。
  - `utterance_activations.pt` shape=`[6194, 2560]`。
  - feature 与 hidden tensor 均为 finite。
  - 9 个核心 MISC 标签覆盖完整，且主标签矩阵不含 `OTHER`。
  - structural metrics 全部 finite，`n_invalid_tokens_skipped=0`。
  - latent-label matrix 行数为 `16384 × 9 = 147456`。
  - AUC、precision、prevalence、Jaccard、weighted Jaccard 等 bounded metrics 在合法范围内。
  - minimal sufficient recoverable 标签的 `minimal_k_median <= candidate_pool_size`。
  - Step6 对照包含 `sae_latents`、`pca_components`、`raw_hidden_dims`。
  - 跨模型汇总覆盖 `llama/gemma` 和全部核心标签。
- 自动迭代记录：
  - 第一次运行 `run_gemma_scope_validation.py` 失败，失败项为：
    - `overlap_thresholded`：把 `top20_jaccard` 在 family-union 不适用行的空值误判为错误。
    - `polysemanticity`：验收脚本期待旧列名 `n_leaf_labels_supported/n_families_supported`，而当前 v2 结果使用 `n_thresholded_labels/n_families`。
  - 复核后确认这是 validation 脚本口径错误，不是实验产物错误。
  - 已修正：
    - Jaccard 检查允许不适用行为空，只校验已定义值必须在 `[0,1]`。
    - polysemanticity 检查兼容新旧列名。
    - thresholded set 统计只计 `stable_edge=True` 的真实 stable edge，不把 `no_stable_latents` sentinel 行计入 stable edge。
  - 第二次运行通过。
- 最终验收命令：
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe -m py_compile run_gemma_scope_validation.py`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe run_gemma_scope_validation.py --root outputs/gemma3_l18_gemmascope_sae_eval`
- 最终验收结果：
  - status=`PASSED`
  - checks=`24`
  - pass=`24`
  - warn=`0`
  - fail=`0`
- 固定产物：
  - `outputs/gemma3_l18_gemmascope_sae_eval/validation/gemma_scope_validation_summary.json`
  - `outputs/gemma3_l18_gemmascope_sae_eval/validation/gemma_scope_validation_report.md`
- 已更新文档：
  - `README.md`
  - `doc/GemmaScope对照实验说明.md`
  - `doc/已获取数据与指标清单.md`

## 2026-06-19 严格口径实验退役与上下文清理

- 用户要求：严格口径相关说明只允许保留在日志中，其他面向 AI/读者的文档不要再出现相关说法，避免污染后续上下文。
- 已执行的清理策略：
  - 删除 `run_misc_latent_space_search_v2.py`、`src/nlp_re_base/latent_space_search_v2.py`、`test_latent_space_search_v2_smoke.py`。
  - 从 `src/nlp_re_base/__init__.py` 移除 `run_latent_space_search_v2` 导出。
  - 保留 `outputs/.../latent_space_search_v2/` 历史产物，不删除现有实验进度；这些文件只作为主线 minimal sufficient 子空间实验的兼容输入。
  - 非日志文档中不再推荐、解释或命名 strict stable-edge / 严格阈值口径；如需追溯历史，只看本日志。
- 当前主线口径：
  - `minimal_sufficient_subspace_v2` 回答预测充分性：每个标签需要多少个 latent 才接近完整候选池表现。
  - Step6 回答结构可解释性对照：SAE / PCA / raw hidden 的结构差异。
  - 这两条仍然只是相关性、结构性和预测充分性证据，不写成因果证明。

## 2026-07-03 最小 latent set 后续实验口径固定

- 已删除并提交 95% Full SAE ratio 最小 latent set 链路；后续不再使用 `0.95 * Full SAE AUC` 作为最小 set 判定标准。
- 后续最小 latent set 实验固定回到 `minimal_sufficient_subspace_v2` 的 full-candidate sufficiency 口径：
  - `AUC >= full_candidate_auc - 0.02`
  - `AUPRC >= full_candidate_auprc - 0.03`
  - `P@50 lift >= full_candidate_precision_lift_at_50 - 0.05`
  - 同时保留 `min_auc >= 0.70` 的 recoverable 下限。
- 当前主线候选池：
  - `outputs/misc_full_sae_eval/functional/misc_label_mapping_filtered/latent_label_matrix.csv`
  - 每个标签 filtered Top100，即 `candidate_policy=filtered_topk_only`、`candidate_top_k=100`。
  - 使用 `feature_filter_audit.csv` 确认候选 latent 均来自 keep=True 池。
- 当前主线输出目录：
  - `outputs/misc_full_sae_eval/interpretability/minimal_sufficient_subspace_v2_filtered`
- 解释边界：
  - 该口径衡量的是 candidate-pool probe-space predictive sufficiency。
  - 不写作 causal sufficiency，也不写成单个 latent 等价于 MISC 标签机制。

## 2026-07-03 AUC / Cohen's d 交叉验证实验执行

- 按 `doc/交叉验证实验计划.md` 新增并执行 filtered-pool 交叉验证链路，主输入固定为：
  - `outputs/misc_full_sae_eval/functional/misc_label_mapping_filtered/feature_filter_audit.csv`
  - `outputs/misc_full_sae_eval/functional/misc_label_mapping_filtered/latent_label_matrix.csv`
  - `outputs/misc_full_sae_eval/feature_store/utterance_features.pt`
  - `outputs/misc_full_sae_eval/label_matrix.csv`
- 新增模块与 runner：
  - `src/nlp_re_base/cross_val_framework.py`
  - `src/nlp_re_base/topk_reproducibility.py`
  - `src/nlp_re_base/effect_size_ci.py`
  - `src/nlp_re_base/cross_quality_val.py`
  - `run_cross_val_topk_reproducibility.py`
  - `run_cross_val_bootstrap_ci.py`
  - `run_cross_val_quality_split.py`
  - `test_cross_val_experiments_smoke.py`
- 验证命令已通过：
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe -m py_compile ...`
  - `C:\Users\chengyu\AppData\Local\miniconda3\envs\qwen-env-py311\python.exe test_cross_val_experiments_smoke.py`
- 已完成实验：
  - E0：生成 `outputs/cross_val/dedup_group_mapping.csv`。
  - E1：按 `source_file` split-half 重算 filtered-pool AUC / Cohen's d，并输出 Top20 Jaccard 和 Spearman。
  - E2：对每标签 Top100 positive Cohen's d latents 做 `source_file` grouped bootstrap CI，`n_bootstrap=2000`。
  - E3：执行 high -> low 与 low -> high 跨质量 split 验证。
- 固定输出：
  - `outputs/cross_val/topk_reproducibility/`
  - `outputs/cross_val/bootstrap_ci/bootstrap_ci_by_label_latent.csv`
  - `outputs/cross_val/cross_quality_validation/`
  - `outputs/cross_val/filtered_pool_association_matrix_with_cv.csv`
  - `outputs/cross_val/cross_val_summary.md`
- 合并后 `cv_candidate_status` 统计：
  - `reproducible_candidate`: 199
  - `cross_quality_risk`: 71
  - `ci_supported_not_cross_quality_tested`: 625
  - `ci_not_available_or_includes_zero`: 113927
- 解释边界：
  - 这些结果增强的是 filtered-pool 关联指标的可复现性和稳定性审计。
  - `reproducible_candidate` 仍是统计候选状态，不等于因果机制或人工确认的 MISC 概念。
