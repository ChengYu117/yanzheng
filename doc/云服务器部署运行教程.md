# 浜戞湇鍔″櫒閮ㄧ讲杩愯鏁欑▼

> 2026-04-27 鏇存柊锛氫簯绔剼鏈粯璁ゆ暟鎹泦宸茬粡鍚屾涓?`data/mi_quality_counseling_misc`銆?
> `deploy/gce/run_full_eval.sh` 浼氳皟鐢ㄦ柊鐗?`run_sae_evaluation.py --data-format auto --label-mode misc_multilabel`锛屼富娴佺▼璇诲彇鍏ㄩ噺 MISC `unit_text`锛屽苟淇濈暀鏍圭洰褰?`candidate_latents.csv` 渚涘洜鏋滈獙璇佺户缁娇鐢ㄣ€?
## 褰撳墠浜戠鏈€鐭彲鎵ц鍛戒护

```bash
cd ~/nlp-re
git fetch origin
git checkout sae-re-project
git pull --ff-only origin sae-re-project
cp -n deploy/gce/env.example deploy/gce/.env
nano deploy/gce/.env
bash deploy/gce/bootstrap.sh
bash deploy/gce/start_full_eval_tmux.sh
tail -f ${SAE_OUTPUT_DIR}/run.log
```

`.env` 鑷冲皯纭锛?
```bash
DATA_DIR=data/mi_quality_counseling_misc
CONDA_ENV_NAME=qwen-env-py311
MODEL_DIR=/mnt/disks/data/models/Llama-3.1-8B
SAE_OUTPUT_DIR=${OUTPUT_ROOT}/sae_eval_full
CAUSAL_OUTPUT_DIR=${OUTPUT_ROOT}/causal_validation_full
```

SAE 瀹屾垚鍚庣户缁洜鏋滈獙璇侊細

```bash
bash deploy/gce/start_causal_tmux.sh
tail -f ${CAUSAL_OUTPUT_DIR}/run.log
```

鏂扮増 SAE 杈撳嚭閲嶇偣鐪嬶細

- `${SAE_OUTPUT_DIR}/dataset_summary.json`
- `${SAE_OUTPUT_DIR}/records.jsonl`
- `${SAE_OUTPUT_DIR}/label_matrix.csv`
- `${SAE_OUTPUT_DIR}/feature_store/utterance_features.pt`
- `${SAE_OUTPUT_DIR}/feature_store/utterance_activations.pt`
- `${SAE_OUTPUT_DIR}/functional/re_binary/`
- `${SAE_OUTPUT_DIR}/functional/misc_label_mapping/`
- `${SAE_OUTPUT_DIR}/candidate_latents.csv`

> 鏇存柊鏃堕棿锛?026-04-23
> 褰撳墠姝ｅ紡涓诲疄楠屾祦绋嬶細`run_sae_evaluation.py -> causal/run_experiment.py`
> 褰撳墠榛樿鏁版嵁闆嗭細`data/mi_re`
> 褰撳墠 SAE checkpoint 璇箟锛氭樉寮忎娇鐢?`--checkpoint-topk-semantics hard`

杩欎唤鏂囨。璇存槑濡備綍鎶婂綋鍓嶉」鐩儴缃插埌涓€鍙?Linux GPU 浜戞湇鍔″櫒锛屽苟璺戦€氭寮忎富瀹為獙涓庡洜鏋滈獙璇佸疄楠屻€備粨搴撻噷鐨勪簯绔剼鏈綅浜?`deploy/gce/`锛岃櫧鐒剁洰褰曞悕鍙?`gce`锛屼絾鍙鏄?Ubuntu GPU 浜戞湇鍔″櫒涔熷彲浠ヤ娇鐢ㄣ€?
---

## 1. 浜戠鑴氭湰鏄惁宸查€傞厤褰撳墠浠ｇ爜

宸查€傞厤銆傚綋鍓嶄簯绔剼鏈拰鏈湴姝ｅ紡娴佺▼淇濇寔涓€鑷达細

- `deploy/gce/env.example`
  - 榛樿 `PYTHON_BIN=python3.11`
  - 榛樿 `DATA_DIR=data/mi_re`
  - 榛樿 `DEVICE=cuda`
  - 榛樿 `CHECKPOINT_TOPK_SEMANTICS=hard`
- `deploy/gce/run_full_eval.sh`
  - 璋冪敤 `run_sae_evaluation.py`
  - 鏄惧紡浼犲叆 `--data-dir "${DATA_DIR}"`
  - 鏄惧紡浼犲叆 `--checkpoint-topk-semantics "${CHECKPOINT_TOPK_SEMANTICS}"`
- `deploy/gce/run_causal.sh`
  - 璋冪敤 `causal/run_experiment.py`
  - 榛樿璇诲彇 `${SAE_OUTPUT_DIR}/candidate_latents.csv`
  - 鏄惧紡浼犲叆鍚屼竴濂?`DATA_DIR` 鍜?`CHECKPOINT_TOPK_SEMANTICS`
- `deploy/gce/bootstrap.sh`
  - 浣跨敤 Python 3.11 鍒涘缓铏氭嫙鐜
  - 瀹夎 `lm-saes` 鍜?`transformer-lens`
  - 鏍￠獙 official SAE backend 鍙鍏?
杩欐剰鍛崇潃浜戠榛樿涓嶅啀璺戣鏂囦唬鐞嗘暟鎹泦锛屼篃涓嶉粯璁よ窇 `cactus`銆俙cactus` 浠嶇劧鍏煎锛屼絾闇€瑕佷綘涓诲姩鏀?`DATA_DIR=data/cactus`銆?
---

## 2. 鎺ㄨ崘鏈嶅姟鍣ㄩ厤缃?
鏈€浣庡缓璁細

- Ubuntu 22.04 鎴?24.04
- 1 寮?NVIDIA GPU
- 24GB 鏄惧瓨鏇寸ǔ
- 100GB 浠ヤ笂纾佺洏
- `nvidia-smi` 鍙敤

鏇寸ǔ鐨勯厤缃細

- L4 / A10 / A100
- 200GB 浠ヤ笂纾佺洏
- 鍗曠嫭鎸傝浇鏁版嵁鐩樺埌 `/mnt/disks/data`

鍏堝湪浜戞湇鍔″櫒涓婃鏌ワ細

```bash
nvidia-smi
python3 --version
git --version
```

濡傛灉 `nvidia-smi` 涓嶅彲鐢紝鍏堝鐞嗛┍鍔ㄥ拰 CUDA 鐜锛屼笉瑕佺户缁窇瀹為獙銆?
---

## 3. 涓婁紶椤圭洰

### 鏂瑰紡 A锛氶€氳繃 GitHub 鎷夊彇

鏈湴鍏堟妸浠ｇ爜鎺ㄥ埌杩滅锛?
```powershell
cd D:\project\NLP_re_dataset_model_base
git status
git add causal/run_experiment.py deploy/gce/run_causal.sh doc/浜戞湇鍔″櫒閮ㄧ讲杩愯鏁欑▼.md
git commit -m "Improve causal validation cloud logging"
git push origin sae-re-project
```

浜戞湇鍔″櫒涓婃媺鍙栵細

```bash
cd ~
git clone -b sae-re-project https://github.com/ChengYu117/yanzheng.git nlp-re
cd ~/nlp-re
```

濡傛灉浜戞湇鍔″櫒涓婂凡缁?clone 杩囷細

```bash
cd ~/nlp-re
git fetch origin
git checkout sae-re-project
git pull --ff-only origin sae-re-project
```

---

## 4. 閰嶇疆浜戠鐜鍙橀噺

澶嶅埗妯℃澘锛?
```bash
cp deploy/gce/env.example deploy/gce/.env
nano deploy/gce/.env
```

鑷冲皯纭杩欎簺椤癸細

```bash
DATA_ROOT=/mnt/disks/data
MODEL_DIR=${DATA_ROOT}/models/Llama-3.1-8B
HF_HOME=${DATA_ROOT}/hf-cache
OUTPUT_ROOT=${DATA_ROOT}/outputs

VENV_DIR=${HOME}/nlp-re/.venv
PYTHON_BIN=python3.11
PYTORCH_INDEX_URL=https://download.pytorch.org/whl/cu124
CONDA_ENV_NAME=
CONDA_BASE=

DATA_DIR=data/mi_re
DEVICE=cuda
CHECKPOINT_TOPK_SEMANTICS=hard

HF_TOKEN=<浣犵殑 Hugging Face token>
MODEL_HF_REPO_ID=meta-llama/Llama-3.1-8B
MODEL_REVISION=main
HF_ENDPOINT=https://hf-mirror.com
HF_HUB_DOWNLOAD_TIMEOUT=60
HF_HUB_ETAG_TIMEOUT=60
HF_HUB_DOWNLOAD_MAX_WORKERS=4

SAE_OUTPUT_DIR=${OUTPUT_ROOT}/sae_eval_full
CAUSAL_OUTPUT_DIR=${OUTPUT_ROOT}/causal_validation_full
```

濡傛灉娌℃湁鎸傝浇 `/mnt/disks/data`锛屽彲浠ユ敼鎴愶細

```bash
DATA_ROOT=${HOME}/data
```

濡傛灉浜戞湇鍔″櫒涓婁篃浣跨敤宸茬粡寤哄ソ鐨?conda 鐜锛屼笉瑕佸彧鍦?SSH 閲屾墜鍔?`conda activate`锛岃€屾槸鍦?`deploy/gce/.env` 閲岃缃細

```bash
CONDA_ENV_NAME=qwen-env-py311
# 濡傛灉鑴氭湰鏃犳硶鑷姩鍙戠幇 conda锛屽啀鏄惧紡濉啓锛屼緥濡傦細
# CONDA_BASE=/root/miniconda3
```

璁剧疆 `CONDA_ENV_NAME` 鍚庯紝`deploy/gce/*.sh` 浼氳嚜鍔ㄦ縺娲昏繖涓?conda 鐜锛涘鏋滅暀绌猴紝鍒欎娇鐢?`${VENV_DIR}`銆?
褰撳墠浠ｇ爜瑕佹眰 official `lm-saes` backend銆傝淇濇寔 `PYTHON_BIN=python3.11`锛屽惁鍒?`requirements.txt` 涓殑 `lm-saes` 鍜?`transformer-lens` 涓嶄細瀹夎銆?
---

## 5. 鍒濆鍖栫幆澧?
鎵ц锛?
```bash
bash deploy/gce/bootstrap.sh
```

杩欎釜鑴氭湰浼氬仛杩欎簺浜嬶細

- 瀹夎 tmux銆乬it
- 濡傛灉 `CONDA_ENV_NAME` 涓虹┖锛氬畨瑁?Python 3.11銆乿env銆乸ip锛屽苟鍒涘缓 `${VENV_DIR}`
- 濡傛灉 `CONDA_ENV_NAME` 闈炵┖锛氭縺娲诲凡鏈?conda 鐜锛屽苟鍦ㄨ鐜閲屽畨瑁呬緷璧?- 瀹夎 PyTorch
- 瀹夎 `requirements.txt`
- 鎵ц `pip install -e .`
- 鏍￠獙 `torch`銆乣lm_saes`銆乣transformer_lens`
- 鏍￠獙涓変釜鍏ュ彛鑴氭湰鐨?`--help`

鎴愬姛鏃朵細鐪嬪埌锛?
```text
Bootstrap completed successfully.
```

甯歌澶辫触鐐癸細

- `deploy/gce/.env` 涓嶅瓨鍦?- Ubuntu 婧愭棤娉曞畨瑁?Python 3.11
- 缂哄皯 `python3.11-venv`锛屽鑷?`ensurepip is not available`
- 缃戠粶鏃犳硶璁块棶 PyPI 鎴?PyTorch wheel 婧?- GPU 椹卞姩涓嶅彲鐢?
濡傛灉鐪嬪埌杩欎釜閿欒锛?
```text
The virtual environment was not created successfully because ensurepip is not available.
apt install python3.11-venv
Failing command: ['/root/nlp-re/.venv/bin/python3.11', '-Im', 'ensurepip', ...]
```

鍦ㄤ簯鏈嶅姟鍣ㄤ笂鎵ц锛?
```bash
cd ~/nlp-re
apt-get update
apt-get install -y python3.11-venv
rm -rf .venv
bash deploy/gce/bootstrap.sh
```

濡傛灉鎻愮ず鎵句笉鍒?`python3.11-venv`锛屽厛鍔?deadsnakes 婧愬悗閲嶈瘯锛?
```bash
apt-get update
apt-get install -y software-properties-common
add-apt-repository -y ppa:deadsnakes/ppa
apt-get update
apt-get install -y python3.11 python3.11-venv
cd ~/nlp-re
rm -rf .venv
bash deploy/gce/bootstrap.sh
```

---

## 6. 涓嬭浇鍩虹妯″瀷

鍥藉唴浜戞湇鍔″櫒寤鸿鍏堝垏鎹?Hugging Face 闀滃儚绔偣銆傜紪杈?`.env`锛?
```bash
cd ~/nlp-re
nano deploy/gce/.env
```

纭鏈夎繖浜涢厤缃細

```bash
HF_TOKEN=<浣犵殑 Hugging Face token>
HF_ENDPOINT=https://hf-mirror.com
HF_HUB_DOWNLOAD_TIMEOUT=60
HF_HUB_ETAG_TIMEOUT=60
HF_HUB_DOWNLOAD_MAX_WORKERS=4
```

鎵ц锛?
```bash
bash deploy/gce/download_model.sh
```
澶辫触灏憋細
cd ~/nlp-re
echo 'HF_ENDPOINT=https://hf-mirror.com' >> deploy/gce/.env
echo 'HF_HUB_DOWNLOAD_TIMEOUT=60' >> deploy/gce/.env
echo 'HF_HUB_ETAG_TIMEOUT=60' >> deploy/gce/.env
echo 'HF_HUB_DOWNLOAD_MAX_WORKERS=4' >> deploy/gce/.env
bash deploy/gce/download_model.sh

榛樿涓嬭浇鍒帮細

```bash
${MODEL_DIR}
```

閫氬父鏄細

```bash
/mnt/disks/data/models/Llama-3.1-8B
```

杩欎竴椤归渶瑕侊細

- `.env` 涓厤缃簡鏈夋晥 `HF_TOKEN`
- Hugging Face 璐﹀彿鏈?`meta-llama/Llama-3.1-8B` 璁块棶鏉冮檺
- 濡傛灉瀹樻柟 Hugging Face 涓嬭浇鍗′綇锛宍.env` 涓缃?`HF_ENDPOINT=https://hf-mirror.com`

濡傛灉涓嬭浇杩囩▼涓暱鏃堕棿娌℃湁杩涘睍锛屽彲浠ヤ腑鏂悗閲嶆柊鎵ц銆俙snapshot_download` 浼氬鐢?`${HF_HOME}` 鍜?`${MODEL_DIR}` 涓凡缁忎笅杞界殑缂撳瓨锛?
```bash
cd ~/nlp-re
HF_ENDPOINT=https://hf-mirror.com bash deploy/gce/download_model.sh
```

濡傛灉杩樻槸鍗′綇锛岄檷浣庡苟鍙戝悗閲嶈瘯锛?
```bash
cd ~/nlp-re
export HF_ENDPOINT=https://hf-mirror.com
export HF_HUB_DOWNLOAD_MAX_WORKERS=1
bash deploy/gce/download_model.sh
```

濡傛灉鎶ラ敊鍫嗘爤閲屼粛鐒跺嚭鐜帮細

```text
from huggingface_hub import login
login(token=token, add_to_git_credential=False)
```

璇存槑浜戞湇鍔″櫒涓婅繍琛岀殑杩樻槸鏃х増 `deploy/gce/download_model.sh`锛屼笉鏄暅鍍忛厤缃棶棰樸€傚厛鎵ц涓嬮潰鐨勬帓鏌ュ懡浠わ細

```bash
cd ~/nlp-re
pwd
readlink -f deploy/gce/download_model.sh
grep -n "login(" deploy/gce/download_model.sh || true
grep -n "snapshot_download" deploy/gce/download_model.sh
source deploy/gce/common.sh
load_gce_env
echo "PROJECT_ROOT=$PROJECT_ROOT"
echo "ENV_FILE=$ENV_FILE"
echo "HF_ENDPOINT=$HF_ENDPOINT"
grep -n "HF_ENDPOINT" "$ENV_FILE" || true
```

濡傛灉 `grep -n "login(" deploy/gce/download_model.sh` 鏈夎緭鍑猴紝鐩存帴鍦ㄤ簯鏈嶅姟鍣ㄤ笂瑕嗙洊鎴愭柊鐗堣剼鏈細

```bash
cd ~/nlp-re
cat > deploy/gce/download_model.sh <<'EOF'
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck disable=SC1091
source "${SCRIPT_DIR}/common.sh"

load_gce_env
activate_venv
ensure_repo_root

echo "PROJECT_ROOT=${PROJECT_ROOT}"
echo "ENV_FILE=${ENV_FILE}"
echo "MODEL_DIR=${MODEL_DIR}"
echo "HF_HOME=${HF_HOME}"
echo "HF_ENDPOINT=${HF_ENDPOINT:-https://huggingface.co}"

if [[ -z "${HF_TOKEN:-}" ]]; then
  echo "HF_TOKEN is empty. Set it in ${ENV_FILE} before downloading the model." >&2
  exit 1
fi

mkdir -p "${MODEL_DIR}" "${HF_HOME}"

python - <<'PY'
import os
from huggingface_hub import snapshot_download

token = os.environ["HF_TOKEN"]
repo_id = os.environ["MODEL_HF_REPO_ID"]
revision = os.environ.get("MODEL_REVISION") or None
model_dir = os.environ["MODEL_DIR"]
cache_dir = os.environ["HF_HOME"]
endpoint = os.environ.get("HF_ENDPOINT") or "https://huggingface.co"
max_workers = int(os.environ.get("HF_HUB_DOWNLOAD_MAX_WORKERS") or "4")

print(f"Downloading {repo_id} to {model_dir}")
print(f"HF_ENDPOINT={endpoint}")
print(f"HF_HOME={cache_dir}")
print(f"max_workers={max_workers}")
snapshot_download(
    repo_id=repo_id,
    revision=revision,
    local_dir=model_dir,
    cache_dir=cache_dir,
    token=token,
    max_workers=max_workers,
)
print("Model download complete.")
PY
EOF
chmod +x deploy/gce/download_model.sh
```

鐒跺悗纭 `.env` 鍙湁涓€濂楁渶缁堢敓鏁堢殑闀滃儚閰嶇疆锛?
```bash
cd ~/nlp-re
grep -n "HF_ENDPOINT" deploy/gce/.env
grep -n "HF_HUB_DOWNLOAD_MAX_WORKERS" deploy/gce/.env
```

濡傛灉鏈夊琛岄噸澶嶉厤缃紝浠ユ渶鍚庝竴琛屼负鍑嗐€傚缓璁洿鎺ラ噸鍐欎负锛?
```bash
sed -i '/^HF_ENDPOINT=/d;/^HF_HUB_DOWNLOAD_TIMEOUT=/d;/^HF_HUB_ETAG_TIMEOUT=/d;/^HF_HUB_DOWNLOAD_MAX_WORKERS=/d' deploy/gce/.env
cat >> deploy/gce/.env <<'EOF'
HF_ENDPOINT=https://hf-mirror.com
HF_HUB_DOWNLOAD_TIMEOUT=60
HF_HUB_ETAG_TIMEOUT=60
HF_HUB_DOWNLOAD_MAX_WORKERS=1
EOF
bash deploy/gce/download_model.sh
```

濡傛灉鎬€鐤戞湁涓や釜椤圭洰鐩綍鎴栦袱涓?`.env`锛屾墽琛岋細

```bash
find /root /mnt -path '*/deploy/gce/.env' -print 2>/dev/null
find /root /mnt -path '*/deploy/gce/download_model.sh' -print 2>/dev/null
```

`download_model.sh` 榛樿璇诲彇鐨勬槸瀹冭嚜宸卞悓鐩綍涓嬬殑 `.env`锛屼篃灏辨槸 `deploy/gce/.env`銆傞櫎闈炰綘鏄惧紡璁剧疆浜?`ENV_FILE=/path/to/other.env`銆?
---

## 7. 鏈€灏忔鏌?
鍔犺浇鐜骞剁‘璁ゅ叧閿彉閲忥細

```bash
source deploy/gce/common.sh
load_gce_env
echo "$MODEL_DIR"
echo "$DATA_DIR"
echo "$SAE_OUTPUT_DIR"
echo "$CAUSAL_OUTPUT_DIR"
echo "$CHECKPOINT_TOPK_SEMANTICS"
```

纭鍏ュ彛鑴氭湰鍙敤锛?
```bash
source deploy/gce/common.sh
load_gce_env
activate_venv
python run_sae_evaluation.py --help
python causal/run_experiment.py --help
python run_ai_re_judge.py --help
```

---

## 8. 璺?SAE 涓诲疄楠?
鍓嶅彴杩愯锛?
```bash
bash deploy/gce/run_full_eval.sh
```

鎺ㄨ崘鐢?tmux 鍚庡彴杩愯锛?
```bash
bash deploy/gce/start_full_eval_tmux.sh
```

鏌ョ湅鐘舵€侊細

```bash
tmux ls
tmux attach -t sae_eval
```

鏌ョ湅鏃ュ織锛?
```bash
tail -f ${SAE_OUTPUT_DIR}/run.log
```

瀹為檯鎵ц鐨勫叧閿弬鏁扮瓑浠蜂簬锛?
```bash
python -u run_sae_evaluation.py \
  --model-dir "${MODEL_DIR}" \
  --device "${DEVICE}" \
  --data-dir "${DATA_DIR}" \
  --output-dir "${SAE_OUTPUT_DIR}" \
  --batch-size "${SAE_BATCH_SIZE}" \
  --max-seq-len "${SAE_MAX_SEQ_LEN}" \
  --checkpoint-topk-semantics "${CHECKPOINT_TOPK_SEMANTICS}" \
  --full-structural
```

鎴愬姛鍚庨噸鐐圭湅锛?
- `${SAE_OUTPUT_DIR}/metrics_structural.json`
- `${SAE_OUTPUT_DIR}/metrics_ce_kl.json`
- `${SAE_OUTPUT_DIR}/metrics_functional.json`
- `${SAE_OUTPUT_DIR}/candidate_latents.csv`
- `${SAE_OUTPUT_DIR}/judge_bundle/`
- `${SAE_OUTPUT_DIR}/latent_cards/`

---

## 9. 璺戝洜鏋滈獙璇佸疄楠?
鍏堢‘淇?SAE 涓诲疄楠屽凡缁忕敓鎴愶細

```bash
${SAE_OUTPUT_DIR}/candidate_latents.csv
```

鍓嶅彴杩愯锛?
```bash
bash deploy/gce/run_causal.sh
```

鎺ㄨ崘鐢?tmux 鍚庡彴杩愯锛?
```bash
bash deploy/gce/start_causal_tmux.sh
```

鏌ョ湅鏃ュ織锛?
```bash
tail -f ${CAUSAL_OUTPUT_DIR}/run.log
```

鍥犳灉楠岃瘉鐜板湪浼氶澶栧啓鍑烘洿缁嗙殑杩愯鐘舵€佹枃浠讹細

```bash
tail -f ${CAUSAL_OUTPUT_DIR}/causal_run.log
cat ${CAUSAL_OUTPUT_DIR}/run_status.json
tail -n 20 ${CAUSAL_OUTPUT_DIR}/run_events.jsonl
cat ${CAUSAL_OUTPUT_DIR}/fatal_traceback.log
```

杩欎簺鏂囦欢鐨勪綔鐢細

- `run.log`锛歚deploy/gce/run_causal.sh` 澶栧眰鑴氭湰鏃ュ織锛屽寘鍚?stdout 鍜?stderr銆?- `causal_run.log`锛歚causal/run_experiment.py` 鍐呴儴 tee 鏃ュ織锛岃褰曟瘡涓樁娈靛拰 batch 杩涘害銆?- `run_status.json`锛氬綋鍓嶉樁娈点€佽€楁椂銆佸け璐ラ樁娈点€佸紓甯哥被鍨嬪拰 traceback銆傞暱鏃堕棿杩愯鏃朵紭鍏堢湅杩欎釜鏂囦欢銆?- `run_events.jsonl`锛氶樁娈靛紑濮嬨€佺粨鏉熴€佸け璐ョ殑浜嬩欢娴侊紝閫傚悎鍒ゆ柇鏈€鍚庡畬鎴愬埌浜嗗摢涓€姝ャ€?- `fatal_traceback.log`锛歅ython fatal error / native crash 鐨勫厹搴曞爢鏍堬紱濡傛灉绋嬪簭琚郴缁?OOM kill锛岃繖閲屽彲鑳戒负绌猴紝闇€瑕佸啀鐪?`dmesg`銆?
濡傛灉浠诲姟鐪嬭捣鏉モ€滆嚜宸卞仠浜嗏€濓紝鎸夎繖涓『搴忔帓鏌ワ細

```bash
cat ${CAUSAL_OUTPUT_DIR}/run_status.json
tail -n 80 ${CAUSAL_OUTPUT_DIR}/causal_run.log
tail -n 80 ${CAUSAL_OUTPUT_DIR}/run.log
dmesg -T | tail -n 80
```

瀹為檯鎵ц鐨勫叧閿弬鏁扮瓑浠蜂簬锛?
```bash
python -u causal/run_experiment.py \
  --model-dir "${MODEL_DIR}" \
  --device "${DEVICE}" \
  --candidate-csv "${SAE_OUTPUT_DIR}/candidate_latents.csv" \
  --data-dir "${DATA_DIR}" \
  --output-dir "${CAUSAL_OUTPUT_DIR}" \
  --batch-size "${CAUSAL_BATCH_SIZE}" \
  --max-seq-len "${CAUSAL_MAX_SEQ_LEN}" \
  --checkpoint-topk-semantics "${CHECKPOINT_TOPK_SEMANTICS}" \
  --lambdas 0.5 1.0 1.5 2.0
```

鎴愬姛鍚庨噸鐐圭湅锛?
- `${CAUSAL_OUTPUT_DIR}/run_status.json`
- `${CAUSAL_OUTPUT_DIR}/run_events.jsonl`
- `${CAUSAL_OUTPUT_DIR}/causal_run.log`
- `${CAUSAL_OUTPUT_DIR}/fatal_traceback.log`
- `${CAUSAL_OUTPUT_DIR}/selected_groups.json`
- `${CAUSAL_OUTPUT_DIR}/results_necessity.json`
- `${CAUSAL_OUTPUT_DIR}/results_sufficiency.json`
- `${CAUSAL_OUTPUT_DIR}/results_selectivity.json`
- `${CAUSAL_OUTPUT_DIR}/results_group.json`
- `${CAUSAL_OUTPUT_DIR}/summary_tables.md`

---

## 10. 鍙€夛細璺?AI 涓撳浠ｇ悊璇勫

濡傛灉瑕佽窇 AI judge锛屽湪 `.env` 涓ˉ鍏咃細

```bash
OPENAI_API_KEY=<浣犵殑 API key>
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_MODEL=qwen3.5-plus
OPENAI_EXTRA_BODY_JSON='{"enable_thinking":false}'

JUDGE_TOP_LATENTS=20
JUDGE_TOP_N=10
JUDGE_CONTROL_N=5
JUDGE_GROUPS=G1,G5,G20
JUDGE_REQUEST_TIMEOUT=600
```

鍓嶅彴杩愯锛?
```bash
bash deploy/gce/run_ai_judge.sh
```

鍚庡彴杩愯锛?
```bash
bash deploy/gce/start_ai_judge_tmux.sh
```

榛樿杈撳叆锛?
```bash
${SAE_OUTPUT_DIR}
```

榛樿杈撳嚭锛?
```bash
${SAE_OUTPUT_DIR}/ai_judge
```

濡傛灉浣犱娇鐢ㄥ叾浠栧吋瀹?OpenAI API 鐨勬湇鍔★紝鍙璁剧疆瀵瑰簲鐨?`OPENAI_BASE_URL`銆乣OPENAI_MODEL` 鍜屽繀瑕佺殑 `OPENAI_EXTRA_BODY_JSON` 鍗冲彲銆?
---

## 11. 涓€娆″畬鏁存墽琛岄『搴?
鏂版湇鍔″櫒涓婃帹鑽愭寜杩欎釜椤哄簭锛?
```bash
cd ~/nlp-re
cp deploy/gce/env.example deploy/gce/.env
nano deploy/gce/.env

bash deploy/gce/bootstrap.sh
bash deploy/gce/download_model.sh

bash deploy/gce/start_full_eval_tmux.sh
```

绛?SAE 涓诲疄楠屽畬鎴愬悗锛?
```bash
bash deploy/gce/start_causal_tmux.sh
```

濡傛灉杩樿璺?AI judge锛?
```bash
bash deploy/gce/start_ai_judge_tmux.sh
```

---

## 12. 濡備綍鍒囨崲鏁版嵁闆?
褰撳墠姝ｅ紡榛樿鏄細

```bash
DATA_DIR=data/mi_re
```

濡傛灉浣犳槑纭璺戝吋瀹圭殑 CACTUS 灏忔暟鎹泦锛屼慨鏀?`.env`锛?
```bash
DATA_DIR=data/cactus
```

鎴栬€呬复鏃惰鐩栵細

```bash
bash deploy/gce/run_full_eval.sh --data-dir data/cactus
bash deploy/gce/run_causal.sh --data-dir data/cactus
```

娉ㄦ剰锛氭寮忔姤鍛婂彛寰勫綋鍓嶄互 `data/mi_re` 涓哄噯锛宍cactus` 涓嶅啀鏄粯璁や富娴佺▼銆?
---

## 13. 甯歌闂

### `lm-saes is not installed`

閫氬父鏄?Python 鐗堟湰涓嶅銆傜‘璁わ細

```bash
grep PYTHON_BIN deploy/gce/.env
```

搴斾负锛?
```bash
PYTHON_BIN=python3.11
```

鐒跺悗閲嶆柊鎵ц锛?
```bash
bash deploy/gce/bootstrap.sh
```

### `HF_TOKEN is empty`

璇存槑 `.env` 閲岃繕娌℃湁濉?Hugging Face token銆傚～濂藉悗閲嶆柊璺戯細

```bash
bash deploy/gce/download_model.sh
```

### `Local model path not found`

璇存槑 `${MODEL_DIR}` 涓嶅瓨鍦ㄦ垨妯″瀷娌′笅杞藉畬鏁淬€傛鏌ワ細

```bash
echo "$MODEL_DIR"
ls -lah "$MODEL_DIR"
```

蹇呰鏃堕噸鏂版墽琛岋細

```bash
bash deploy/gce/download_model.sh
```

### `Couldn't instantiate the backend tokenizer`

濡傛灉 `run_full_eval.sh` 鍦?`[1/7] Loading base model...` 鎶ワ細

```text
ValueError: Couldn't instantiate the backend tokenizer ...
You need to have sentencepiece or tiktoken installed
```

鍏堟鏌ヤ緷璧栵細

```bash
cd ~/nlp-re
source deploy/gce/common.sh
load_gce_env
activate_venv
python - <<'PY'
import importlib.util
for name in ("sentencepiece", "tiktoken", "protobuf"):
    print(name, importlib.util.find_spec(name) is not None)
PY
```

濡傛灉鏈?`False`锛岃ˉ瑁咃細

```bash
python -m pip install -U sentencepiece tiktoken protobuf
```

鐒跺悗妫€鏌ユā鍨嬬洰褰曟槸鍚︿笅杞藉畬鏁达細

```bash
ls -lh "$MODEL_DIR" | head
ls "$MODEL_DIR"/tokenizer* "$MODEL_DIR"/config.json 2>/dev/null
```

Llama 3.1 鏈湴鐩綍鑷冲皯搴旇鑳界湅鍒扮被浼硷細

```text
config.json
tokenizer.json
tokenizer_config.json
special_tokens_map.json
model-*.safetensors
```

濡傛灉缂哄皯 `tokenizer.json` 鎴栨潈閲嶆枃浠讹紝璇存槑妯″瀷鐩綍涓嶅畬鏁淬€傞噸鏂拌蛋闀滃儚涓嬭浇锛?
```bash
cd ~/nlp-re
export HF_ENDPOINT=https://hf-mirror.com
export HF_HUB_DOWNLOAD_MAX_WORKERS=1
bash deploy/gce/download_model.sh
```

濡傛灉鐩綍閲屾湁娈嬬己鏂囦欢锛屽彲浠ュ厛澶囦唤鏃х洰褰曞啀閲嶆柊涓嬭浇锛?
```bash
mv "$MODEL_DIR" "${MODEL_DIR}.broken.$(date +%Y%m%d_%H%M%S)"
mkdir -p "$MODEL_DIR"
bash deploy/gce/download_model.sh
```

### `Candidate CSV not found`

璇存槑鍥犳灉瀹為獙鍚姩鏃舵病鏈夋壘鍒帮細

```bash
${SAE_OUTPUT_DIR}/candidate_latents.csv
```

鍏堣窇瀹岋細

```bash
bash deploy/gce/run_full_eval.sh
```

### CUDA 鏄惧瓨涓嶈冻

浼樺厛鍦?`.env` 閲岃皟灏忥細

```bash
SAE_BATCH_SIZE=2
CAUSAL_BATCH_SIZE=2
SAE_MAX_SEQ_LEN=128
CAUSAL_MAX_SEQ_LEN=128
```

濡傛灉浠嶇劧涓嶈冻锛屽彲浠ョ户缁妸 batch size 璋冨埌 `1`銆?
### SSH 鏂紑

闀夸换鍔′笉瑕佺洿鎺ュ湪鏅€?SSH 鍓嶅彴璺戙€備娇鐢細

```bash
tmux ls
tmux attach -t sae_eval
tmux attach -t causal_eval
tmux attach -t ai_judge
```

---

## 14. 涓€鍙ヨ瘽鎬荤粨

浜戞湇鍔″櫒涓婅窇閫氬綋鍓嶅疄楠屽氨鏄繖鍑犳锛?
1. 涓婁紶浠ｇ爜
2. 濉ソ `deploy/gce/.env`
3. 鎵ц `bootstrap.sh`
4. 鎵ц `download_model.sh`
5. 鐢?`start_full_eval_tmux.sh` 璺?SAE 涓诲疄楠?6. 鐢?`start_causal_tmux.sh` 璺戝洜鏋滈獙璇?7. 鍙€夌敤 `start_ai_judge_tmux.sh` 璺?AI 涓撳浠ｇ悊璇勫

鏈€閲嶈鐨勪笁涓彛寰勬槸锛?
- `PYTHON_BIN=python3.11`
- `DATA_DIR=data/mi_re`
- `CHECKPOINT_TOPK_SEMANTICS=hard`

---

## 15. 灏嗘湰鍦颁慨鏀规帹閫佸埌浜戠

濡傛灉浜戞湇鍔″櫒鏄€氳繃 Git clone 閮ㄧ讲鐨勶紝鎺ㄨ崘鐢?GitHub 浣滀负涓浆锛?
```bash
# 鏈湴鎻愪氦骞舵帹閫?git status
git add causal/run_experiment.py deploy/gce/run_causal.sh doc/浜戞湇鍔″櫒閮ㄧ讲杩愯鏁欑▼.md
git commit -m "Improve causal validation logging and cloud deployment notes"
git push origin sae-re-project
```

鐒跺悗鍦ㄤ簯鏈嶅姟鍣ㄤ笂鎷夊彇鏈€鏂颁唬鐮侊細

```bash
cd ~/nlp-re
git fetch origin
git checkout sae-re-project
git pull --ff-only origin sae-re-project
```

鎷夊彇鍚庡缓璁厛纭鍏ュ彛鍙敤锛?
```bash
source deploy/gce/common.sh
load_gce_env
activate_venv
python causal/run_experiment.py --help
```

濡傛灉浜戞湇鍔″櫒娌℃湁 Git 杩滅鏉冮檺锛屼篃鍙互鍦ㄦ湰鍦版墦鍖呭悗涓婁紶锛?
```powershell
python package_project.py
```

鍐嶆妸鐢熸垚鐨?`tar.gz` 涓婁紶鍒颁簯鏈嶅姟鍣ㄨВ鍘嬭鐩栥€備笉瑕佷笂浼?`outputs/`銆佹ā鍨嬬洰褰曘€佹棩蹇楃洰褰曟垨鏈湴 wheel 缂撳瓨锛涜繖浜涙枃浠朵綋绉ぇ锛岃€屼笖浼氳浜戠浠ｇ爜鐘舵€佸彉寰楅毦杩借釜銆?

09485f0 Sync cloud scripts for official SAE runtime
origin/sae-re-project
鏈嶅姟鍣ㄤ笂杩欐牱鏇存柊锛?
bash

cd ~/nlp-re
git fetch origin
git checkout sae-re-project
git pull --ff-only origin sae-re-project
鐒跺悗纭 .env 閲屾槸鍚﹁鍚敤浣犵殑鏂?conda 鐜锛?
bash

nano deploy/gce/.env
濡傛灉浜戞湇鍔″櫒涔熺敤 conda锛屽姞鍏ユ垨淇敼锛?
bash

CONDA_ENV_NAME=qwen-env-py311
# 濡傛灉鑷姩鎵句笉鍒?conda锛屽啀濉細
# CONDA_BASE=/root/miniconda3
鏇存柊渚濊禆骞堕獙璇佸畼鏂?SAE 鐜锛?
bash

bash deploy/gce/bootstrap.sh
妫€鏌ュ綋鍓嶈剼鏈疄闄呮縺娲荤殑鐜锛?
bash

source deploy/gce/common.sh
load_gce_env
activate_venv
print_runtime_summary
璺戜富瀹為獙锛?
bash

bash deploy/gce/run_full_eval.sh
鎴栧悗鍙拌窇锛?
bash

bash deploy/gce/start_full_eval_tmux.sh
tail -f ${SAE_OUTPUT_DIR}/run.log
濡傛灉鏈嶅姟鍣ㄤ笂 git pull 鎻愮ず鏈湴鏀瑰姩鍐茬獊锛屽厛鍒己鍒惰鐩栵紝鎵ц锛?
bash

git status --short
